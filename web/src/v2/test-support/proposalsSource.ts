// Текст вкладки «Предложения» целиком — для сторожей, читающих разметку (`?raw`).
// Строка предложения вынесена в свой файл (шип 5 §8 d: экран не длиннее 600
// строк): сторож смотрит на обе части разом, иначе перенос «прятал» бы
// проверяемое.
import вкладка from '../knowledge/proposals.tsx?raw'
import строка from '../knowledge/proposalrow.tsx?raw'

export const ТЕКСТ_ПРЕДЛОЖЕНИЙ = [вкладка, строка].join('\n')
