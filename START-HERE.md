# НАЧНИТЕ ОТСЮДА

Информационная система поддержки разработки космической системы IoT.
Ранние фазы жизненного цикла (Pre-Phase A / Phase A по NPR 7120.5, NPR 7123.1):
выработка требований к элементам КС и передача пакета в детальное проектирование.

## Что в репозитории

| Каталог | Что это | Читать |
| --- | --- | --- |
| `README.md` | Как поднять и что смотреть. Для тех, кто видит проект впервые | если не разработчик |
| `CLAUDE.md` | Правила работы, жёсткие ограничения Р1–Р9, глоссарий | **первым** |
| `STRUCTURE.md` | Структура репозитория и принципы разложения | вторым |
| `docs/tz/` | ТЗ: 75 требований с исполняемыми критериями приёмки | по мере работы |
| `docs/adr/` | 25 решений с обоснованиями. Нарушать нельзя | при сомнениях |
| `docs/arch/` | Архитектура: конвейер расчётов, стек, риски | перед кодом |
| `docs/model/` | Концепция и модельные документы — «почему так» | для контекста |
| `docs/ui/` | Промпты для Stitch: 9 экранов, токены, итерации | при работе над `web/` |
| `schemas/` | 22 JSON-схемы — **нормативная** структура данных | постоянно |
| `spec/` | Исполняемая спецификация поведения (602 проверки) | постоянно |
| `tools/` | Проверки документов, в продукт не входят | в CI |
| `db/` | DDL и ключевые запросы | шаг 1 |
| `core/` | Ядро Kotlin/JVM: модуль = префикс ТЗ | реализация |
| `web/` | Клиент TypeScript/React + CesiumJS | позже |
| `ci/checks.sh` | Полный набор проверок | перед каждым коммитом |

## Одно имя на всех уровнях

Требование `TZ-KA-005` → описано в `docs/tz/TZ-KA.md` → реализуется в `core/ka/`.
Префикс идентификатора одновременно служит именем файла ТЗ и именем модуля.
Таблиц соответствия нет и не нужно.

## Посмотреть, что получилось

```bash
docker compose up --build      # открыть http://localhost:8080
```

Поднимается база, демонстрационный проект «Орбита-IoT», API ядра и клиент.
Что смотреть на стенде первым — `README.md`; подробности и выкат на сервер —
`ops/README.md`.

## Первое, что сделать

1. Прочитать `CLAUDE.md` — там порядок работы и ограничения, нарушение которых является ошибкой независимо от формулировки задачи.
2. Поднять базу: `bash ci/local_db.sh` — часть проверок идёт по реальной схеме.
3. Выполнить `bash ci/checks.sh` — должно быть «ВСЕ ПРОВЕРКИ ПРОЙДЕНЫ».
4. Открыть задание текущего шага в `docs/tz/` (см. ниже).

## Текущий шаг

Шаги 1–15 выполнены; Шаг 16 — блоки A и B (приёмка §4 — впереди).
Задания: `docs/tz/STEP-1.md` … `STEP-6.md`, `STEP-7-9.md`, `STEP-10-13.md`,
`STEP-14.md`, `STEP-15.md`, `STEP-16.md` (+ `STEP-16-ИНВЕНТАРИЗАЦИЯ.md`).

| Шаг | Что сделано |
| --- | --- |
| 1–2 | Модель, схемы, хранилище, контур требований и трассировки |
| 3 | Расчётный контур: адаптер протокола, видимость, зоны обслуживания |
| 4 | Монте-Карло потоков, замыкание KPI, модель столкновений |
| 5 | ИИ-контур `TZ-AI` со структурным фильтром предложений |
| 6 | Выходной контур и клиент: экраны 3, 3б, 11 |
| 7 | Реестр рисков и демо-проект «Орбита-IoT» |
| 8 | Экраны 7, 12, 6 (сравнение, обзор, баллистика) |
| 9 | Мастер Ш1–Ш7 целиком, сквозной проход, `docs/tz/CR-005-008.md` |
| 10 | CR-005…008; стенд одной командой; карта спроса из внешнего датасета; библиотеки заполнены |
| 11 | Документы по приложениям БП-PA; экспорт ReqIF (ADR-023); пакет передачи; отчёт зрелости к SRR |
| 12 | Наземный сегмент (рекомендательное размещение), валидация баллистики TAT-C, ADR-025 |
| 13 | Полный масштаб TZ-COM-004 (36 с из 300, ADR-026); экраны на полном проекте; отчёт о состоянии |
| 14 | Импорт — третий канал (ADR-024): правовой режим источников, тот же фильтр, происхождение |
| 15 | Рабочий слой: ввод и правка через интерфейс, оптимистичная блокировка, ИИ-контур |

Шаги 1–15 выполнены. Состояние проекта, ограничения и открытые вопросы —
`docs/STATUS.md`.

Ключевое: файлы `spec/*.py` — не примеры, а **эталоны поведения**.
Все проверки должны быть воспроизведены в тестах один в один.
Расхождение реализации с эталоном — дефект реализации, а не эталона.
