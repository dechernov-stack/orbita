-- Библиотека (ЗАДАЧА-CODE-БИБЛИОТЕКА §3): экземпляр в проекте связан с
-- библиотечным прототипом связью «применяет». Статус применимости и
-- обоснование живут в документе экземпляра (поле applies) — связь служит
-- навигации и подсветке применений при новой редакции.
ALTER TYPE link_kind ADD VALUE IF NOT EXISTS 'applies';
