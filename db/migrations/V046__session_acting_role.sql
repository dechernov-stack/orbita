-- ADR-066: владелец системы выступает от имени роли (РП · ведущий СИ · инженер · DA),
-- выбор живёт в сессии и переживает перезапуск; каждое действие журналируется
-- автором «<имя> как <роль>».
ALTER TABLE sessions ADD COLUMN acting_role text;
