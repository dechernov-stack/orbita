// Публичный контракт модуля «documents» (слой L5).
//
// Порт-контракт документов: документ, раздел, элемент, рендеринг, выпуск, результат.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.documents.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.documents.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "documents"
    const val LAYER: String = "L5"
    const val PURPOSE: String = "Порт-контракт документов: документ, раздел, элемент, рендеринг, выпуск, результат."
}
