// Публичный контракт модуля «ai» (слой сквозной).
//
// Виды пакетов, сборка промпта по источникам, канал пакетов, внешний контур, живые вызовы с журналом.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.ai.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.ai.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "ai"
    const val LAYER: String = "сквозной"
    const val PURPOSE: String = "Виды пакетов, сборка промпта по источникам, канал пакетов, внешний контур, живые вызовы с журналом."
}
