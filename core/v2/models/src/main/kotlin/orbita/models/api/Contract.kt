// Публичный контракт модуля «models» (слой L4).
//
// Модели системы, прогоны, метрики, бюджеты с резервами, варианты построения, оценка засорения.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.models.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.models.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "models"
    const val LAYER: String = "L4"
    const val PURPOSE: String = "Модели системы, прогоны, метрики, бюджеты с резервами, варианты построения, оценка засорения."
}
