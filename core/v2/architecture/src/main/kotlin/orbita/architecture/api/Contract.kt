// Публичный контракт модуля «architecture» (слой L3).
//
// Компоненты с гранями, параметры, стыки, функции, цепочки, режимы, элементы обмена, конфигурационные единицы.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.architecture.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.architecture.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "architecture"
    const val LAYER: String = "L3"
    const val PURPOSE: String = "Компоненты с гранями, параметры, стыки, функции, цепочки, режимы, элементы обмена, конфигурационные единицы."
}
