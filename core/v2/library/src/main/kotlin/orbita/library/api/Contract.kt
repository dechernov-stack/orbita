// Публичный контракт модуля «library» (слой L0).
//
// Полки и шаблоны: PBS, стыки, Arcadia, WBS, шаблоны компонентов, шаблон фазы, каталог процессов; окно взятия.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.library.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.library.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "library"
    const val LAYER: String = "L0"
    const val PURPOSE: String = "Полки и шаблоны: PBS, стыки, Arcadia, WBS, шаблоны компонентов, шаблон фазы, каталог процессов; окно взятия."
}
