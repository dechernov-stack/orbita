// Публичный контракт модуля «knowledge» (слой L2).
//
// Материалы, канон и карта разбора, задания загрузки, факты, темы, допущения.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.knowledge.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.knowledge.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "knowledge"
    const val LAYER: String = "L2"
    const val PURPOSE: String = "Материалы, канон и карта разбора, задания загрузки, факты, темы, допущения."
}
