// Публичный контракт модуля «kernel» (слой ядро).
//
// Ядро сущности: id, code, kind, area, born_in, статус, версия, провенанс, история; связи реестром; схемы из YAML.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.kernel.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.kernel.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "kernel"
    const val LAYER: String = "ядро"
    const val PURPOSE: String = "Ядро сущности: id, code, kind, area, born_in, статус, версия, провенанс, история; связи реестром; схемы из YAML."
}
