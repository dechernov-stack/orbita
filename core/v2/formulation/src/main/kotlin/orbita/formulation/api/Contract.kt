// Публичный контракт модуля «formulation» (слой L2).
//
// Замысел, стейкхолдеры, нужды, цели, ограничения, сервисы; матрица покрытия.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.formulation.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.formulation.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "formulation"
    const val LAYER: String = "L2"
    const val PURPOSE: String = "Замысел, стейкхолдеры, нужды, цели, ограничения, сервисы; матрица покрытия."
}
