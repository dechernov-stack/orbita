// Публичный контракт модуля «readiness» (слой L1).
//
// Каталог проверок, разрывы, лестница зрелости, TRL-шлюз; реализует оценщик ворот.
//
// Правило ТЗ-BACKEND §2.1: другие модули видят ТОЛЬКО этот пакет.
// Реализация живёт в orbita.readiness.internal и наружу не выносится —
// это проверяет архитектурный тест, а не договорённость между людьми.
package orbita.readiness.api

/**
 * Опознание модуля: имя, слой, назначение.
 *
 * Волне 0 он нужен якорем каркаса: по нему архитектурный тест видит, что
 * модуль заведён, объявил слой и имеет публичный пакет. Дальше сюда придут
 * настоящие порты модуля.
 */
object ModuleInfo {
    const val NAME: String = "readiness"
    const val LAYER: String = "L1"
    const val PURPOSE: String = "Каталог проверок, разрывы, лестница зрелости, TRL-шлюз; реализует оценщик ворот."
}
