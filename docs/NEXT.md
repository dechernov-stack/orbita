# NEXT — живое состояние и открытые вопросы

Обновляется по ходу работ; история — в git.

## Идёт сейчас

Пачка-1 передачи (`docs/tz/manual-run-2/пачка-1/`): шип 1 — фиксы Ф-01…Ф-03
(тег mvp-fix-08) и шип 2 — Д1, разбор документа при загрузке (ADR-032:
MD-канон с якорями + JSON-карта, числа каноном, отпечаток по хешу; экран
«Библиотека → Разбор документов») — выкачены. Все четыре документа стенда
разобраны. Д2 — смысловой разбор одним вызовом (вид пакета
`document_semantic_parse`, правила в реестре видов, вкладка «Найдено в
документе», акцепт урожая по адресам) — выкачен; шаг Б2 ПМИ исполняется
вставкой эталонного `ПАКЕТ-РАЗБОР-ЗАПИСКИ.json`. Б-03 не создавался.
Ответы владельца по позициям Д1/Д2 исполнены: приёмка переведена на
синтетические фикстуры (примеры в репозитории не лежат — он публичен),
география и вехи ложатся заготовками с честной пустотой (область без
геометрии даёт разрыв готовности карты спроса; веха — без даты, с
примечанием-происхождением).
Ф-01 закрыт обоими пунктами (створка воспроизведена, опора календаря через
границу фаз считается сервером), Ф-05 исполнен: промпт постановки собирается
из данных — замысел миссии обязателен, библиотека класса, взятое и принятое
входят позициями, состав виден предпросмотром, пустой источник назван.
Ф-06 исполнен (ADR-034): анкеты характеристик носителей — данными полки,
три пути заполнения на форме, даташит предзаполняет поля с координатами
блоков, незаполненное обязательное — разрыв готовности.
Пачка-1 закрыта целиком (Ф-01…Ф-06, Д1–Д3). Пачка-2 исполнена: умолчание
«в промпт» по типу документа (Ф-08.1), замысел из документов с якорями
(Ф-07), полоса-проводник и метки источников (Ф-08.2–.3), зрелость анкет по
точкам (Ф-06 п.5), шаблон записки на полке. Замечание к пачке-2 закрыто
(тег `mvp-p2-02`): семантика меток источников выправлена дословно по записке
владельца — **[И]** внутренний документ, **[В]** внешний источник,
проверенный на указанную дату, **[П]** предлагаемая цель или допущение,
требующее подтверждения; редакция правил промпта поднята до 2 и печатается
в промпте, сторож `SourceMarkSemanticsTest` держит формулировку, шаблон
`DT-9001` на стенде обновлён до в. 2. Условие «Е6 [Ж] — только после этой
правки» выполнено.

Пачка-3 исполнена (Ф-09, Ф-10; ADR-035): библиотека вошла в контур ИИ —
источник промпта «знание полки» отдаёт пункты нормативов и блоки канонов
вместо перечня кодов, документ полки участвует в промпте выбранными блоками
с умолчанием по типу, норматив порождает кандидатов требований и ограничений
с обязательным основанием (`normative_to_candidates`), а знания выгружаются
во внешний контур архивом MD с отпечатком: инструкция генерируется из реестра
видов, вставка пакета по старой выгрузке предупреждена.

Ф-11 исполнен (ADR-036): мастер-путь идёт в порядке знания — параметры →
материалы → замысел → запуск ИИ; «собрать из документов» на шаге замысла
живая, а без материалов ведёт назад к их загрузке. Правило-класс: неактивный
контрол обязан нести причину и путь оживления — сторож подсказок падает на
голом `disabled` (закрыто 30 кнопок в 17 файлах).

«Работа фазы» доведена кругами владельца: круг 1 — карточка задачи с полосой
шагов; круг 2 — лента окнами от дат вех и ярусами зависимостей; круг 3 — рамка
ведения (задача как рабочее место: степпер сверху, встроенный экран шага снизу,
собственных форм у рамки нет — сторож `tools/validate_task_frame.py`); круг 4
(ADR-038) — третий вид «Схема»: карта потока фазы. Узлы-задачи ярусами, рёбра
подписаны артефактами и открывают документ, точки ромбами с процентом ИЗ
готовности (клик ведёт к готовности именно этой точки — маршрут получил
параметр `gate`), за последними воротами — следующая фаза облаком. Схема
считается каждый раз и не хранится: сторож `tools/validate_flow_computed.py`
запрещает перетаскивание, обмер DOM, память координат и запись из проекции.
Живость — последняя активность и люди аватарками (служебные учётки на схему не
выходят), фильтр «за неделю». В рамке ведения появилась нить потока: «вход →
задача → выход → кого кормишь», переходы живые.

Круг 5 (ADR-039) — Гант библиотекой: решение владельца «без лишнего фанатизма,
взять с Git» исполнено на frappe-gantt 1.2.2 (MIT). Самодельная лента и вся её
пиксельная геометрия удалены; полосы, стрелки зависимостей, перетаскивание дат,
шкала и режимы День/Неделя/Месяц — из коробки. Наше — данные и правила: даты
полосы берутся из ПЛАНА руководителя (поле паспорта `work_plan`, право —
руководитель проекта, отказ называет право), а где плана нет — полоса остаётся
расчётной сеткой, серой и подписанной «план не задан». План не влияет на
статусы; «план против факта» — красной окантовкой, а не заливкой; конфликт со
стрелкой подсвечен с обеих сторон, соседей никто не двигает; процентов
выполнения нет (`progress` 0 и скрыт стилем). Сторож
`tools/validate_gantt_library.py` держит границу: самострой поверх библиотеки и
её форк — отказ сборки.

Круг 6 (ADR-040) — связи, вехи, подзадачи: тип связи стал данными полки
(FS · SS · FF · INPUT, тип обязателен — сторож сида), и он решает, чего задача
ждёт, каков её ярус и что считается конфликтом плана. Phase A размечена по
смыслу регламента: 2 и 3 от SEMP — SS, 5–9 от архитектуры — SS, 11 от 3/4/10 —
FF, чистый FS остался внутри задачи 11. Вехи пошли вертикалями через полотно
(механизм holidays библиотеки), расхождение дат с порядком прохождения
печатается «конфликтом вех». Шаги стали подзадачами полотна: свёрнуты по
умолчанию, раскрываются из попапа, порядок — связями `after` (SS рядом, FS
ярусом ниже), планов шагам не заводят, клик шага ведёт в рамку на нём.

Круг 7 (ADR-041) — строки и интервалы: шаги стали полноправными строками
полотна («1.1 · …», развёрнуто по умолчанию, свернуть щелчком по сводной
полосе, память на сессию), план переехал на ШАГ, а полоса задачи с шагами
стала сводной и не тянется. Фаза поделена точками на интервалы (начало фазы →
SRR → SDR → KDP-B): задачи без плана размечаются внутри своего интервала,
поэтому ромб стоит в его конце по построению. Задача, чьё окно уходит за свою
точку, — конфликт с обеих сторон; дата точки раньше начала интервала —
«конфликт вех» с переходом на жизненный цикл.

Круг 8 (ADR-042) — критерий связей и таблица строк: все 16 связей Phase A
пересмотрены по вопросу «может ли задача ЗАКОНЧИТЬСЯ без выхода той» (FS 1→2,
1→3, 1→10; SS 4→5…9; FF 2→3, 3→4, 3→10, 4→10, 3/4/10→11; INPUT 9→10), критерий
записан в note каждой связи и проверяется сторожем. Связь шага может уходить в
чужую задачу (2.3 ждёт 1.3). Слева от полотна встала таблица строк (№ · работа,
ответственный, длительность рабочими днями, статус текстом) — сосед канвы, не
графика поверх неё; имена на полотне не рисуются. Ответственный за работу стал
полем проекта (назначает руководитель, шаг наследует задачу), «Моя работа»
показывает и задания, и работы. Прогресс задачи вычисляется из закрытых шагов —
ручного процента по-прежнему нет. Найден и починен старый дефект: наш класс
`.bar` перебивал геометрию полос библиотеки, и полотно с круга 5 рисовало все
полосы шириной 90 px независимо от дат.

**Открытый вопрос владельцу:** разделить задачу 1 Phase A на «Развёртывание
фазы» (О1, шлюз) и «SEMP» (О2, параллельная) — рекомендация «разделять»,
перенумерация контента за владельцем.

Патч контента Phase A (КОНТЕНТ-PHASE-A-ПАТЧ.md, решение владельца): задача 1
разделена на «1 · Развёртывание фазы» (О1 — шлюз фазы: орг-структура и роли,
план работ фазы) и «2 · SEMP» (О2 — параллельный документ с базированием до
SRR: по шаблону → связные разделы службой `semp_draft` → базировать). Прежние
задачи сдвинуты на один номер (12 задач); идентификаторы полки стабильны — SEMP
получил новый `PW-0112`, порядок несёт поле `order`. Все связи пересмотрены по
критерию круга 8 с `note`: FS 1→2, 1→3, 1→4; FF 2→3, 3→4, 4→5, 4/5/10→11(12);
SS только 5→6…10; INPUT 10→11. Связи шагов через границу: 2.3 → 3.3 и 2.3 → 4.3
(порядок базирования: SEMP раньше ConOps и требований). Условие `objects`
получило необязательный `code`: «связные разделы SEMP написаны» считает только
тексты разделов шаблона semp. Сидер создаёт новые объекты пакета и считает
полку устаревшей, если в сиде есть задача, которой на полке нет.

«Три пакета» (ЗАДАЧА-CODE-ТРИ-ПАКЕТА.md), шип 0 исполнен: печать docx/PDF идёт
человеческим текстом (PrintHumanizer: веха «SRR — 02.09.2026, не проведена»,
узел именем без id, поля по-русски, перечисления справочником EnumLabels,
единицы справочником UnitLabels); выпуск отказывает документу с латинским
служебным ключом в печатном тексте, статический сторож
`tools/validate_print_keys.py` сверяет словарь подписей с ключами генератора;
полка описания среды: Orekit — в резерве, расчёты — свои; Г-02: состав пачки
и счётчик кнопки — одна функция `chosenRows` с тестом; реестр маршрутов
`docs/ui/routes.md` генерируется из nav.ts и App.tsx (`tools/routes_registry.py
--check` в CI). Дальше шипы 1–3 (связный текст у [С]-раздела, остаток пачки
SEMP, сиды SEH); шип 4 (заимствования ReqPilot) — только по команде.

Шипы 1–2 «трёх пакетов» (ADR-043): режим раздела — поле шаблона (table · prose ·
prose_table · manual), «Написать связно» у любого [С]-раздела экрана документов
(вид `section_prose`, вход собирает сервер из данных вставок раздела
человеческими строками), правка и явное принятие текста, печать по режиму
(принятый текст + [Т]-таблицы), «текст устарел» с дифом строк («было/стало»),
SEMP ред. 2 на 11 разделов (нормативы, 17 процессов с tailoring, TPM из
свёрток, история выпусков), одна сборка модели документа `DocumentModel`.
Шипы 2.3–2.4 тоже исполнены: Библиотека → «Результаты» (карточки выпусков с
авторством из истории текстов, служба автором не бывает; «обобщить в образец»
→ template_extraction с текстом выпуска), Г-01 — диалог сопоставления и на
загрузке пачкой, карта применяется при импорте, `tools/slice_packet.py` для
нарезки срезов под другой проект. Шип 3 (сиды SEH) оказался уже исполнен
прежними заходами: П13–П20 в правилах `services_to_requirements` (ред. 2),
линт L-C1…C6 в `Quality.kt` со словарём полки и полем срока TBD, чек обзора
полкой (шаг 12.1 ведёт к инспекции), глоссарий 41 записью с источником и
отчётом коллизий. Шип 4 (заимствования ReqPilot) — только по команде владельца.

Стенд: учётки выключены (`{"enabled":false}`) после сброса пароля — первая
регистрация вернёт роли руководителя. Новые проекты владельца (PJ-0003, PJ-0004) удалены по его просьбе
2026-08-30 — прогон начинается заново; на стенде остались PJ-0001 (история
второго прохода ПМИ) и PJ-0002 с четырьмя разобранными документами.

Живой ручной прогон по ПМИ (`docs/tz/manual-run/ПМИ.md`), второй заход:
инженер — Дмитрий, ведущий испытаний — Code. Пройдены internal_review, MCR,
KDP-A; проект в Phase A, движение к SRR. Находки чинятся по ходу; журнал
находок — в сообщениях коммитов (`git log --grep="Находка"`).

## Открытые вопросы

* **Стейкхолдер с несколькими ролями** (замечание живого прохода ПМИ-3,
  владелец: «не критично, надо поправить потом»). Сейчас `role` — одно
  значение из перечня, а один и тот же участник бывает разом регулятором,
  заказчиком и потребителем: ГКРЧ и Роскомнадзор регулируют и потребляют,
  Минтранс заказывает и регулирует. Правка: `role` → массив ролей с
  сохранением совместимости (одиночное значение читается как список из
  одного), матрица группирует по каждой роли, обобщение в профиль А2 берёт
  главную. Задевает схему стейкхолдера, матрицу покрытия, заведение
  носителей из нужд и профиль полки — поэтому отдельным шипом, не правкой
  на ходу.

* **Дизайн формы карточки объекта** (поставлен инженером на втором заходе,
  2026-08-25). Сделанное по ходу прогона — вкладки «Определение / Готовность /
  История», действия и препятствия сверху, поля-ссылки списками, ужатие
  таблицы при открытой карточке — снимает остроту, но карточка остаётся
  сырой формой по схеме: без группировки полей по смыслу (постановка /
  показатель / верификация), без разумной ширины колонок, подформы событий
  верификации громоздки. Нужен осознанный проход дизайном (пользователь:
  «дизайном займёмся позднее» — по всему приложению разом, эскизы
  docs/ui/reference как основа).

* **Автопростановка происхождения из документа-источника** (ADR-030,
  отложено): материал, порождённый службой из исходного документа SD-NNNN,
  пока не несёт `provenance.import.dataset = SD@версия` автоматически.

## После прогона

* Заполнить таблицы §4–§5 ПМИ по журналу сессии, отчёт `прогон-<дата>.md`.
* Замечания дизайна всего приложения (отложены пользователем до конца
  прогона).
