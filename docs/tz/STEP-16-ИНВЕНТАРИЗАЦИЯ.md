# Шаг 16 §2.1. Инвентаризация неподключённого

Список получен `ci/wiring.py --init` (не руками) и лежит в `ci/unwired.txt`:
**28 маршрутов и 21 функция**. По каждому элементу — исход «подключить» или
«удалить», третьего задание не даёт. Ниже обоснование по каждому.

Проверено по коду, а не по названиям: для маршрута — есть ли потребитель хоть
где-нибудь в `web/src` (не только в `web/src/api/*.ts`), для функции — есть ли
вызывающий вне `src/test`. Совпадения по смыслу (два входа в одно и то же)
сверялись чтением обработчиков.

---

## 1. Маршруты

### 1.1. Подключить (24)

| Маршрут | Чем подтверждён | Куда |
| --- | --- | --- |
| `/reports/trace-matrix` | TZ-OUT-004 | требования, вкладка «Трассировка» (§2.4) |
| `/reports/verification-matrix` | TZ-OUT-004 | требования, вкладка «Верификация»; ответ перевести на `verificationMatrixView` — см. §2 |
| `/reports/validation-matrix` | TZ-OUT-004 | требования, вкладка «Валидация» |
| `/reports/maturity` | TZ-OUT-003 | готовность к точке, основная таблица |
| `/reports/review-candidates` | TZ-OUT-003 | готовность к точке, перечень к рассмотрению |
| `/reports/trace-breaks` | TZ-OUT-002 | система в целом, раздел «Проблемы» |
| `/reports/inconsistent-allocations` | TZ-OUT-002 | там же |
| `/reports/elements-without-requirements` | TZ-OUT-002 | там же |
| `/reports/needs-without-services` | TZ-OUT-002 | нужды и сервисы, предупреждение на экране |
| `/reports/stale-results` | TZ-MOD-007 | сравнение вариантов, пометка «результат устарел» |
| `/reports/unaccepted-ai` | TZ-AI | предложение ИИ, список неакцептованного |
| `/views/spacecraft/mask-schedule` | TZ-KA-009, Р4/ADR-004 | экран КА: сгенерированные доли витка рядом с ручными |
| `/ground/suggest` | шаг 12.1, Концепция 5.4 | наземный сегмент: подбор поверх хранимых станций |
| `/import/terminal-profile` | ADR-024 | импорт записи каталога, по одной, по действию инженера |
| `/import/reqif` | ADR-024 | импорт требований, черновики тем же фильтром |
| `/export/reqif` | TZ-OUT-005, ADR-023 | требования: выгрузка файла |
| `/export/package` | TZ-OUT-006 | **уже вызывается** из `web/src/screens/Readiness.tsx` мимо слоя `api` — перенести вызов в `web/src/api/client.ts` |
| `/protocol-adapter` | TZ-NET-001, TZ-NET-006 | моделирование: параметры канала отдаёт только адаптер |
| `/params/{имя}` | TZ-MOD-005 | карточка элемента: значение, единица, формула, происхождение |
| `/param-deps` | TZ-MOD-005 | там же: зависимость параметра от параметра |
| `/objects/{id}/ancestors` | TZ-REQ-003 | навигация по связям из карточки (§3.3: от требования к элементу и обратно) |
| `/objects/{id}/descendants` | TZ-REQ-003 | там же |
| `/objects/{id}/change` | TZ-COM-003 | карточка базированного объекта: изменение с основанием. **Первоначально был помечен на удаление — ошибочно**, см. §4 |
| `/links` | §3.1 задания | **только `kind=verification`**: три остальных вида после ADR-027 выводятся из документа и вручную не создаются. Экран верификации привязывает событие к требованию |

### 1.2. Удалить (4)

| Маршрут | Почему дубликат |
| --- | --- |
| `/components/{CM}/specification` | отдаёт `req.specificationOf(cm)`; подключённый `/views/components/{CM}` отдаёт то же самое внутри карточки (`screens.componentSpecification` вызывает ту же функцию) |
| `/reports/risk-register` | отдаёт `registerSummary(risks)`; подключённый `/views/risks` отдаёт `{summary: registerSummary(risks), …}` — строгое подмножество. Сверка, которую требует §2.4, дала совпадение |
| `/validate/{схема}` | второй вход в ту же проверку: сохранение уже отвечает 422 с тем же перечнем ошибок (`SchemaValidationException`). Отдельный «проверить, не сохраняя» разойдётся с проверкой при сохранении |
| `/slice?at=…` | экрана нет и в ТЗ не заявлен: срез на дату требуется как **операция хранилища** (STEP-1 §1.6, `ObjectStore.sliceAt`), она остаётся и покрыта тестами. Удаляется только HTTP-вход |

---

## 2. Функции представления

### 2.1. Подключить (17)

| Функция | Чем подтверждена | Куда |
| --- | --- | --- |
| `coverageByTarget` (`Coverage.kt`) | §2.1 задания | `/views/coverage`; **вызывающих нет вообще, включая тесты** — покрыть эталоном |
| `availabilityHeatmap` (`VizData.kt`) | §2.2 задания | `/views/coverage`: сетка на трёх горизонтах, среднее и худшее окно |
| `availability` (`VizData.kt`) | ловушка 3 в самой функции | там же: среднесуточная, **взвешенная профилем активности**. Простое среднее по часам завышает качество сервиса, когда пик спроса приходится на провал покрытия |
| `cellCriticality` (`ComparisonViews.kt`) | §2.2, ловушка 2 | класс ячейки считает сервер, клиент красит по классу |
| `verificationMatrixView` (`Presentation.kt`) | §2.4 | маршрут `/reports/verification-matrix` переводится на неё: строка на пару «требование × событие», разрывы — отдельным списком, а не пустой ячейкой |
| `unverifiedRequirements` (`Matrices.kt`) | TZ-REQ-008 | вкладка «Верификация»: непокрытые требования |
| `notExecuted` (`Presentation.kt`) | TZ-OUT-002 | там же: запланировано, но не выполнено |
| `bottlenecks` (`Presentation.kt`) | TZ-FLW | моделирование: узкие места из сохранённого прогона |
| `tpmOutsideReserves` (`Presentation.kt`) | TZ-COM-003 | готовность к точке: TPM вне резервов |
| `treeCycle` (`Tree.kt`) | TZ-REQ-003 | «Проблемы»: цикл подчинённости |
| `render` (`Documents.kt`) | TZ-OUT-001 | документы БП-PA из выгрузки модели |
| `toCsv` (`Exchange.kt`) | **TZ-OUT-005: «в ReqIF и CSV»** | требования: выгрузка CSV. Незнакомый атрибут получает колонку, а не теряется |
| `toExchange` (`Exchange.kt`) | вход для `toCsv` | сборка выгрузки из модели |
| `exchangeToJson` (`Exchange.kt`) | TZ-OUT-005 | сериализация выгрузки |
| `mappingIssues`, `flattenedAsString` (`ReqifMapping.kt`) | ADR-023 | предупреждения отображения возвращаются вместе с выгрузкой ReqIF: файл валиден, а перечислимое поле, отданное строкой, лишает принимающий инструмент фильтрации |
| `datatypeDefinitions` (`ReqifMapping.kt`) | ADR-023 | `ReqifExport.payload` сейчас берёт виды атрибутов из `REQUIREMENT_MAP` напрямую — свести к одному входу через эту функцию |

### 2.2. Удалить (5)

| Функция | Почему дубликат |
| --- | --- |
| `kpiRose` (`VizData.kt`) | нормировка розы уже идёт через `radarSeries`, который вызывает `comparisonView` (`ComparisonViews.kt:40`) — это второй вход в ту же логику. Две реализации нормировки разойдутся на первом изменении осей (§2.1, ловушка 1) |
| `kpiRoseJson` (`VizData.kt`) | сериализация того же второго входа |
| `fromExchange` (`Exchange.kt`) | обратное направление формата `reqif-lite/1.0`. Импорт после ADR-023 идёт настоящим ReqIF через службу обмена (`/import/reqif`), второго формата ввода в системе нет |
| `exchangeFromJson` (`Exchange.kt`) | разбор того же формата ввода |
| `radarOption` (`VizData.kt`) | всплыл после удаления `kpiRose`: других вызывающих у него не было. Экран сравнения собирает `RadarOption` из ХРАНИМОГО результата, беря оси, которые в нём фактически есть (`HttpApi.kt:748`) — второе отображение расходилось бы с ним |

---

## 3. Что требует решения владельца

1. **Удаление четырёх функций затрагивает эталон.** `Exchange.kt` и `VizData.kt`
   покрыты `spec/presentation_semantics.py`: удаление `fromExchange`,
   `exchangeFromJson`, `kpiRose`, `kpiRoseJson` требует правки эталона — по
   правилу 4 `CLAUDE.md` отдельным коммитом, не вместе с кодом. Эталон правится
   вслед за решением, а не «чтобы прошло».
2. **Четыре маршрута удаляются вместе с их тестами**, если такие есть.
   Функциональность при этом не теряется: у каждого назван действующий вход.
3. **Номера ADR из §5 задания заняты**: ADR-025 — атмосферные потери,
   ADR-026 — полный масштаб TZ-COM-004. По правилу «номера стабильны и не
   перенумеровываются» новые решения получают **ADR-027** (связи из документа)
   и **ADR-028** (пустой стенд по умолчанию).
4. **Область обхода §1.1 расширена одним файлом**: `coverageByTarget` назван в
   задании поимённо, но лежит в `core/bal/Coverage.kt`, вне «`core/out` +
   `VizData.kt`». Без расширения строку было бы нечем вычеркнуть.

Итог: **41 подключить, 9 удалить** (49 элементов списка плюс `radarOption`,
всплывший при удалении). Ни одной строки «оставить на будущее».

---

## 4. Поправка по `/objects/{id}/change`

В первой редакции этого отчёта маршрут был помечен на удаление как «второй вход
записи версии» — рабочий слой `/edit/*` из Шага 15 выглядел его заменой.
Это неверно, и видно это из кода самого рабочего слоя:

```kotlin
if (cur.status == Lifecycle.Baseline) {
    throw BaselineEditBlockedException(
        id, "объект базирован: изменение через процедуру с основанием",
    )
}
```
(`Editing.kt:88`)

Рабочий слой базированный объект НЕ правит и отсылает к процедуре с основанием —
а единственный её вход это и есть `POST /objects/{id}/change` с `change_ref`.
Удаление оставило бы TZ-COM-003 без входа: изменить базированное требование
через интерфейс стало бы нечем. Маршрут возвращён и переведён в «подключить»;
поймано при правке теста, который эту процедуру и проверяет
(`HttpApiTest.kt`, «изменение Baseline без основания отклоняется 409»).

---

## 5. Поправки по ходу блока A (§2.4)

Сверка «два способа получить одно» при подключении отчётов дала ещё четыре
дубликата — по правилу §2.1 они удалены, а не выведены на экран:

| Маршрут | Действующий вход |
| --- | --- |
| `/reports/needs-without-services` | `/views/needs`: пустой `services` в строке — тот же факт, экран уже фильтрует и подсвечивает |
| `/reports/trace-breaks` | `systemOverview.problems` — единый вход раздела «Проблемы»; перечень дополнен разрывами |
| `/reports/inconsistent-allocations` | там же |
| `/reports/elements-without-requirements` | там же (перечень уже включал элементы без требований) |

`treeCycle` подключён туда же — цикл подчинённости попадает в «Проблемы».
Два источника одного перечня разошлись бы молча; строковый перечень
`systemOverview` выбран действующим входом, потому что он уже на экране.
