# Отчёт · круг 2 стартового потока (даты вех и исходные документы)

Эталоны владельца — обновлённой поставкой в `reference2/` (отказ порядка
дат в create-project S3; карточка исходного документа в project-start Ш2).
Версия стенда: тег `stand-round2-01`.

## 1. Порядок дат вех

- **Одно правило на сервере** (правила паспорта в ReqService): действует
  на создании проекта, в паспорте и в канале правки с основанием —
  StartFlowRound2Test. Живой стенд: обратные даты → 422
  «MCR не может быть раньше внутреннего обзора — даты идут по порядку
  точек», объект не создаётся. Подписи точек — данные
  (gate-labels-ru.json).
- Частично заданные даты законны (КТ-2 без КТ-1 — не ошибка); «дата не
  задана» — законно всегда.
- Форма: календарь ограничен предыдущей заданной вехой (min), нарушение
  красит поле и даёт warn текстом эталона. Ограничение платформы:
  браузерный календарь не умеет «открыться на дате» без значения —
  min-предел даёт то же поведение выбора.

## 2. Исходные документы: файл + карточка + разбор

- Типология вида: записка миссии · норматив · даташит · справка · прочее;
  вид один, живёт в обеих областях (area=library — полки А3/А4/В2).
- POST /sd-files: файл + карточка (тип и наименование обязательны);
  **текст извлекает сервер** (docx — POI, PDF — PDFBox, txt/md); файл
  хранится в томе orbita-files (подключён к api, входит в копию О-18 —
  backup_stand.sh пакует том). GET /sd-files/{id} отдаёт файл обратно.
- Ш2 «Сложить своё»: форма загрузки (файл, тип, наименование, источник);
  строка файла — «карточка заполнена ✓», карточка раскрытием (тип,
  источник, дата, аннотация) с кнопками «Разобрать: профили
  стейкхолдеров» / «типовые риски» — запуск существующих видов службы,
  профиль подбирается по разрешённым видам; «результат придёт на акцепт»
  (область LIB). Вид «Аннотация документа» заведён (document_annotation).
- AP-0001 стенда → в. 4: виды разбора добавлены процедурой с основанием.

## Приёмка круга

- [x] обратный порядок — отказ сервером и формой с именами точек
      (сервер: живой стенд; форма: warn+подсветка текстом эталона);
- [x] правило одно на сервере, работает и в паспорте (тест);
- [x] записка загружается с карточкой через Ш2 и через /sd-files;
      «Разобрать: профили стейкхолдеров» даёт пакет на акцепт — этим
      закрыт вопрос запуска О-5 (нужна лишь сама записка);
- [x] файлы в резервной копии (том orbita-files в backup_stand.sh);
- [ ] снимки рядом с эталонами — за живым проходом владельца (свой
      пробный файл на чистый стенд не грузил — вы просили чистоту).

## 3. Рейка: «Библиотека» и «Документы» — разделы первого уровня

- Состав рейки: Портфель · Библиотека · Постановка · Требования ·
  Концепция · Модели · Документы · Контроль · Инструменты — девять
  разделов, подписи целиком, иконки из эталонов (книга; лист с загнутым
  углом). Действует на всех экранах: рейка одна (nav.ts).
- «Библиотека»: Материалы проекта (прежний экран исходных документов,
  изъят из «Постановки») + Полки — новый обзор по фактическому
  наполнению: А1 нормативы, А2 стейкхолдеры, Б3 риски, Б4 классы миссий,
  фрагменты Б1/Б2/Б5/Б6/Б7/Г1 со счётчиками состава, В1 шаблоны
  документов. Пустая полка честно видна пустой с подсказкой, откуда
  наполнение (разбор записки — А2/Б3).
- «Документы»: комплект фазы и выпуски — прежний экран docs целиком,
  из «Контроля» пункт изъят. Маршрут не менялся: закладки и переходы
  внутри системы живы (ключ экрана docs прежний, сменилась секция).
- Приёмка: рейка нового состава на живом стенде проверена на экранах
  Портфель/Библиотека/Документы; «Материалы проекта» у PJ-0001 честно
  пусты; «Полки» показывают наполнение LIB.

## Дополнение: красный CI по первому пушу круга

Прогон 9675cbc упал в CI на приёме /sd-files: на машине тестов нет
каталога /files и не было переменной ORBITA_FILES_DIR. Исправление —
в ci/checks.sh (общий для CI и локали): перед тестами ядра каталог
файлов задаётся времянкой, если не задан снаружи. Локальные прогоны
шли с переменной, потому расхождение всплыло только в CI.
