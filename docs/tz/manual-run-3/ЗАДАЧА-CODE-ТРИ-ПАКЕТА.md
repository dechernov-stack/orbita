# Задача Code: три подготовленных пакета одним заходом (02.09.2026)

Сверено по репозиторию на `8a12753`: вид `section_prose`/`semp_draft`
уже есть; сторожа печати, раздела «Результаты», сидов SEH, `reqif` и
`@xyflow/react` — нет. Источники: `ЗАДАЧА-CODE-SEMP.md`,
`ШАБЛОН-SEMP.md`, `SEMP-СВЕРКА-NASA.md`, `ЗАМЕЧАНИЯ-ПМИ3-Г.md`,
`СИД-ПРАВИЛА-ТРЕБОВАНИЙ.md`, `СИД-ГЛОССАРИЙ-SE.md`,
`РЕШЕНИЕ-ЗАИМСТВОВАНИЯ-REQPILOT.md`. Меры — в них; здесь порядок и
связки.

## Шип 0 · Разблокировать прогон (день)

1. **Печать без служебных ключей**: вставки в docx/PDF — человеческим
   текстом (поля по-русски, вехи «SRR — 02.09.2026, не проведён», узлы
   именами); сторож `tools/validate_print_keys.py`: латинский
   служебный ключ (`kind:`, `held:`, `name:`…) в печатном выводе —
   отказ выпуска. Разблокирует И5 и Л8.
2. Полка описания среды: Orekit — «в резерве»; расчёты — свои.
3. Г-02: счётчик кнопки пачки от фактического выбора.
4. Реестр маршрутов файлом в `docs/` — если ещё не выгружен.

## Шип 1 · «Написать связно» у любого [С]-раздела

Действие живёт **у раздела документа**, не у шага задачи: любой
шаблон полки с режимом `[С]` на разделе. Вид общий `section_prose`
(параметры: шаблон · раздел · данные вставок раздела; подсказки
раздела — из полки). `semp_draft` — частный случай: свести к общему
или оставить псевдонимом, по цене. Шаг 2.2 задачи SEMP продолжает
открывать то же действие из рамки.

## Шип 2 · Остаток пачки SEMP

1. **DT-9002 ред. 2** — 11 разделов по `ШАБЛОН-SEMP.md` (если на полке
   ещё 8): «Применимые документы» вставкой из полки нормативов;
   §6 — 17 процессов с tailoring-пометками «вне области»; §8 TPM —
   запасы массы и мощности из свёрток, тренды замечаний точек.
2. **Устаревание с дифом**: данные вставки изменились → «текст устарел»
   + список разошедшихся полей; молчаливой перезаписи текста нет.
3. **Библиотека → «Результаты»**: карточки выпусков — документ ·
   версия · снимок печати · авторы текста из истории правок +
   выпустивший + дата; служба автором не бывает (сторож). Существующие
   выпуски — миграцией; «обобщить в образец» с карточки.
4. **Г-01**: ссылка пакета на чужой проект — диалог сопоставления с
   автопредложением по формулировке; изоляция не ослабляется; нарезка
   срезов перемапливает по смысловому ключу.

## Шип 3 · Сиды SEH

По `ЗАДАЧА-CODE-СИДЫ-SEH.md`: правила П13–П20 в вид
`requirements_generation` (версия правил вверх); линт L-C1…L-C6
мягкими пометами, список слов — данными справочника, к помете TBD —
поле срока; чек обзора полкой чек-листов, шаг «Внутренний обзор»
задачи 12 ведёт к нему, галки с автором; глоссарий 41 записью с
источником, коллизии — списком владельцу, не перезаписью.

## Шип 4 · Заимствования ReqPilot — **после итога прогона, по команде**

По `РЕШЕНИЕ-ЗАИМСТВОВАНИЯ-REQPILOT.md`: контейнер `reqif`
(Apache-2.0, экспорт → импорт с `foreign_attributes`, детерминизм
идентификаторов тестом); граф на `@xyflow/react` + `@dagrejs/dagre`
одним движком для трассировки и схемы потока (elkjs — нет, EPL/GPL);
`conflicts_with` с разрывом «противоречие не разрешено»; автономный
HTML-отчёт. Сторожа границ — по образцу гантовского. Не начинать
раньше команды владельца.

## Сквозная приёмка (шипы 0–3, на проекте ПМИ-3)

- [ ] печать ConOps и SEMP — ни одного служебного ключа; сторож валит
      выпуск на подложенном `kind:` (тест);
- [ ] на [С]-разделе ConOps — «Написать связно»; черновик → правка →
      принятие; в печати — проза;
- [ ] SEMP: §2 таблица НПА с редакциями, §6 — 17 строк, §8 — числа из
      свёрток; правка даты SRR → «устарел» с дифом поля;
- [ ] выпуск SEMP — карточкой в «Результатах» с авторами;
- [ ] срез пакета из чужого проекта вставляется через диалог без правки
      JSON; счётчик пачки честен;
- [ ] реестр требований показывает L-C2 на «при необходимости»; чек
      обзора открывается из шага; глоссарий различает верификацию и
      валидацию; коллизии — отчётом.

## Ловушки

1. Красота против правды: текст, расходящийся с данными, — брак.
2. Автор — человек; служба — происхождение. 3. Режимы разделов и
подсказки — данными полки. 4. Изоляцию проектов не ослаблять.
5. Шип 4 без команды не трогать — прогон важнее.
