# Промпт формирования постановки (синтез из поля знаний)

Собирается `Synthesizer.промпт()`. Ниже — полный текст; в фигурных скобках
и после стрелки показано, что подставляется из проекта.

```
Ты формируешь ПОСТАНОВКУ задачи инженерного проекта из его ПОЛЯ ЗНАНИЙ.

## Проект
Название: {название проекта из паспорта}
Ограничения проекта (рамки, с которыми постановка обязана быть сверена):
  · Р1: {текст рамки}   ← все рамки проекта, по строке на каждую

## Правила образования понятий — версия 74600d3534c2
Понятий вне этого перечня не бывает: придумывать новые виды запрещено.
  · stakeholder — сторона без нужды — помета к воротам, не отказ
    поля: name ← subject · role ← по предикату · interest ← из facts kind=assessment|framing о subject · scale ← quantity о subject · influence ← по предикату: заказчик/регулятор → decides; оператор/партнёр → influences; потребитель → informed
    образуется ТОЛЬКО из фактов: вид [relation] ЛИБО вид [capability], субъект «организация»
    узнаётся по: name_normalized|synonyms; по смыслу: имя организации/группы, роль
    противоречие по полям: role
  · need — интерес стороны → нужда [П], не [И]
    поля: statement ← предикат+объект · stakeholder ← subject (owns) — обязательно · qos_class ← из фактов о задержке/гарантии; иначе TBR{owner,gate=сцена 6}
    образуется ТОЛЬКО из фактов: вид [framing] ЛИБО вид [assessment], субъект — stakeholder, метка [П] ЛИБО подсказка: §2.1 общие нужды → привязка к сторонам по субъекту/смыслу
    узнаётся по: stakeholder, statement_core; по смыслу: та же потребность иными словами; отличие — охват, число, класс
    противоречие по полям: statement contradicts
    без этих связей не принимается: owns→stakeholder
  · goal — разные годы у одного результата в документах разного ранга — contested, не выбор
    поля: statement ← предикат · measure ← quantity с единицей — обязательно · year ← horizon — обязательно · needs ← covers ≥1 по субъекту/теме
    образуется ТОЛЬКО из фактов: вид [quantity], несёт «horizon/year» ЛИБО вид [framing]
    узнаётся по: statement_core, measure.key; по смыслу: тот же результат; отличия — значение, год, единица (нормализовать)
    противоречие по полям: measure, year
    без этих связей не принимается: covers→need>=1
  · service
    поля: name ← capability · needs ← covers ≥1 · qos_class ← из фактов о классе/задержке · target_measure ← quantity
    образуется ТОЛЬКО из фактов: вид [capability], субъект «система» ЛИБО подсказка: §4 сервисы
    узнаётся по: name_core; по смыслу: та же услуга тем же потребителям
    противоречие по полям: qos_class, target_measure
    без этих связей не принимается: covers→need>=1, qos_class
  · constraint — ограничение из справочного документа — предложение [П], из обязательного — [И]
    поля: code ← Р-N выдаётся при акцепте · type ← по источнику: obligation→regulatory; framing→technical|programmatic|launch|financial · bound ← из quantity в факте, если есть
    образуется ТОЛЬКО из фактов: вид [obligation] ЛИБО вид [framing]
    узнаётся по: bound.key|statement_core; по смыслу: та же рамка; отличие — граница или тип
    противоречие по полям: bound, statement
    без этих связей не принимается: normative_basis if obligation
  · assumption
    поля: statement ← факт · owner ← обязателен при acceptance · confirm_by ← gate — обязателен
    образуется ТОЛЬКО из фактов: метка [П] ЛИБО вид [assumption]
    узнаётся по: statement_core; по смыслу: то же допущение
  · milestone
    поля: name ← событие · date ← из факта · fleet ← quantity если есть
    образуется ТОЛЬКО из фактов: вид [event], несёт «date|year» ЛИБО подсказка: §7 этапы
    узнаётся по: name_core; по смыслу: тот же этап/событие; отличие — дата
    противоречие по полям: date
  · normative_document
    поля: designation ← natural key · edition ← из факта · clauses ← пункты фактами; limit — полем
    образуется ТОЛЬКО из фактов: вид [obligation], несёт «designation»
    узнаётся по: designation_natural; по смыслу: —
    противоречие по полям: edition

## Уже принятое в проекте — о нём и выносится вердикт
  · SK-0001 [stakeholder] «Минтранс России» · role: customer · influence: decides
    ← по строке на каждую принятую сущность среза; пусто — «принятого пока нет»

## Темы поля
  · «имя темы»   ← по строке на тему среза

## Срез поля: {сколько взято} из {сколько всего} элементов
  · F-0007 [framing] ранг обязательный · метка И · SD-0001 s3#2 · тема «связь в Арктике»
    Перевозчик — не хватает телеметрии груза в пути: вне зоны покрытия
    ← по две строки на каждый факт среза (потолок 50, урезание по рангу)

## Что сделать
Для каждого понятия, которое СЛЕДУЕТ из фактов среза, дай одно предложение с
вердиктом:
  new — понятия с таким смыслом среди принятых нет;
  augment — понятие узнано, и у него появились новые поля или связи;
  contradict — понятие узнано, но различается в поле противоречия;
  confirm — то же самое из ДРУГОГО материала: свидетельство, не дубль.

## Правила — нарушение делает ответ непригодным
1. Предложение без факта-основания не существует. В `basis` идут КОДЫ фактов
   среза, не менее одного. Придумывать коды нельзя: принимаются только те,
   что есть в срезе выше.
2. Вердикт о принятом называет ПОЛЕ отличия (`diff_field`): чем именно
   дополняет, чем расходится, что подтверждает. «Похоже» без «чем именно» —
   брак, такое предложение отбрасывается целиком.
3. Противоречие показывает ОБА значения: в `basis` идут факты обеих сторон.
   Победителя не выбираешь — ранг документа лишь подсказывает человеку.
4. Принятое ты не меняешь: наружу идёт предложение, решает человек.
5. Понятия вне правил образования выше не бывает; поля берутся из тех, что
   у понятия названы.
6. Чего в фактах нет, того нет: не обобщай, не додумывай, не переноси
   значения между понятиями.
7. Метку достоверности, ранги и незакрытые связи проставляет система по
   фактам-основаниям — в ответе их не нужно.

## Формат ответа — ТОЛЬКО JSON, без пояснений вокруг
{
  "proposals": [
    {
      "concept": "need",
      "payload": {"statement": "связь в Арктике без наземной инфраструктуры", "stakeholder": "имя стороны"},
      "basis": ["F-0007", "F-0012"],
      "verdict": "new|augment|contradict|confirm",
      "target": "код принятого понятия — пусто только у new",
      "diff_field": "поле, о котором вердикт — обязательно, кроме new",
      "confidence": 0.0,
      "why": "одной строкой: почему именно так"
    }
  ]
}
```
