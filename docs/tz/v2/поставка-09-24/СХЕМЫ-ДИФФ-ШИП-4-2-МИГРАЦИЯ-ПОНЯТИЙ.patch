diff --git "a/docs/tz/v2/\320\241\320\245\320\225\320\234\320\253-\320\237\320\236\320\233\320\225\320\231-V2.yaml" "b/docs/tz/v2/\320\241\320\245\320\225\320\234\320\253-\320\237\320\236\320\233\320\225\320\231-V2.yaml"
index 52ac8fd..2ea3717 100644
--- "a/docs/tz/v2/\320\241\320\245\320\225\320\234\320\253-\320\237\320\236\320\233\320\225\320\231-V2.yaml"
+++ "b/docs/tz/v2/\320\241\320\245\320\225\320\234\320\253-\320\237\320\236\320\233\320\225\320\231-V2.yaml"
@@ -226,11 +226,11 @@ kinds:
     required: false
     note: 'порог нормы — поле limit, не число из текста; «Б1: увод ≤ 5 лет» → limit{key:active_lifetime,op:le,value:5,unit:год}'
     label: Пункты
-  - name: source_mark
+  - name: mark
     type: enum[И,В,П]
     required: false
-    note: ''
-    label: Метка источника
+    note: 'ось «происхождение»; прежнее имя source_mark снято'
+    label: Происхождение
   export: SEMP §2; матрица соответствия
   engine: есть
 - n: 6
@@ -300,7 +300,7 @@ kinds:
     note: задействованный персонал стадии
     label: Роли
   - name: scenes
-    type: '[{key*,title*,order*,track*:enum[design,modeling,management],instantiate_per?:enum[project,segment,element],operations*:str[],role*,inputs*:[{kind*,condition*}],output*:{kind|document_template*,maturity*},visibility*:kind[],links*:[{to*,type*:enum[FS,SS,FF,INPUT],rationale*}],steps*:[{key*,title*,hint*,target*:{route*,preset?},condition*}]}]'
+    type: '[{key*,title*,order*,track*:enum[design,modeling,management],instantiate_per?:enum[project,segment,element],operations*:str[],role*,inputs*:[{kind*,condition*}],output*:{kind|document_template*,maturity*},visibility*:kind[],links*:[{to*,type*:enum[FS,SS,FF,INPUT],rationale*}]}]'
     required: true
     note: дорожки и экземпляры на узлы — по схеме Романова
     label: Сцены
@@ -718,35 +718,6 @@ kinds:
     label: Узел
   export: —
   engine: менять
-- n: 21
-  code: step
-  name: шаг сцены
-  layer: L1
-  born_in: по шаблону
-  status_model: 'вычисляется: done по условию'
-  fields:
-  - name: scene
-    type: ref scene
-    required: true
-    note: ''
-    label: Сцена
-  - name: key
-    type: str
-    required: true
-    note: ''
-    label: Ключ
-  - name: responsible
-    type: ref account
-    required: false
-    note: ''
-    label: Ответственный
-  - name: plan
-    type: '{start,end}'
-    required: false
-    note: ''
-    label: План
-  export: —
-  engine: менять
 - n: 22
   code: gate
   name: точка / ворота
@@ -917,7 +888,7 @@ kinds:
   status_model: 'вычисляется: open|done (гаснет закрытием цели)'
   fields:
   - name: target
-    type: '{kind*:enum[gap,step],ref*}'
+    type: '{kind*:enum[gap,activity],ref*}'
     required: true
     note: ''
     label: Цель (объект)
@@ -1096,15 +1067,15 @@ kinds:
   born_in: '2'
   status_model: null
   fields:
-  - name: authority
+  - name: rank
     type: enum[mandatory,expert,reference,doubtful]
     required: true
-    note: ранг доверия источника — обязательный · экспертный · справочный · сомнительный; наследуется фактами; в противоречии подсказывает, решает человек
+    note: ось «доверие» (ОНТОЛОГИЯ-АУДИТ часть 3) — ранг доверия источника, ставит человек при загрузке — обязательный · экспертный · справочный · сомнительный; наследуется фактами; в противоречии подсказывает, решает человек; прежнее имя authority снято
     label: Ранг доверия
   - name: type
     type: enum[mission_memo,tor,directive,normative,datasheet,quote,external_icd,external_model,analysis,heritage,regulatory_decision,reference]
     required: false
-    note: режим разбора (tor · datasheet · normative) выводится заданием и профилем, НЕ поле ввода; доверие — в authority
+    note: режим разбора (tor · datasheet · normative) выводится заданием и профилем, НЕ поле ввода; доверие — в rank
     label: Тип
   - name: title
     type: str
@@ -1231,7 +1202,7 @@ kinds:
     note: ''
     label: Источник
   - name: items
-    type: '[{class*,payload*,anchors?,source_mark?:enum[И,В,П],decision*:enum[pending,accepted,rejected,rework],reason?,target_ref?}]'
+    type: '[{class*,payload*,anchors?,mark?:enum[И,В,П],decision*:enum[pending,accepted,rejected,deferred],reason?,target_ref?}]'
     required: true
     note: ''
     label: Элементы
@@ -1280,10 +1251,10 @@ kinds:
     note: ''
     label: Роль
   - name: interest
-    type: text
+    type: '[{statement*,quote?,fact?: ref fact}]'
     required: true
-    note: ''
-    label: Интерес
+    note: интересы стороны её словами, каждый с цитатой (ОНТОЛОГИЯ-АУДИТ часть 4) — не роль, не нужда; нужда из интереса — решением (предложение через сверку, не автомат); строка прежних записей сходит первым интересом
+    label: Интересы
   - name: scale
     type: measure{op,value|min,max,unit→unit}
     required: false
@@ -1333,11 +1304,12 @@ kinds:
     required: true
     note: ''
     label: Формулировка
-  - name: stakeholder
-    type: ref stakeholder
+  - name: stakeholders
+    type: ref[] stakeholder (≥1)
     required: true
-    note: owns
-    label: Сторона
+    note: носители нужды — связью owns (n:m, ОНТОЛОГИЯ-АУДИТ часть 4); поле зеркалит связи, ставит система; одна формулировка — одна нужда, носителей много
+    label: Стороны
+    required_at: accept:system
   - name: qos_class
     type: ref[] qos_class (≥1) | tbr{owner*,gate*}
     required: true
@@ -3407,26 +3379,21 @@ kinds:
     required: true
     note: документ с якорем — или эксперт без якоря (учётка · роль · дата); ровно одно из двух, стережёт Kotlin
     label: Источник
-  - name: source_mark
+  - name: mark
     type: enum[И,В,П]
     required: true
-    note: ''
-    label: Метка источника
-  - name: source_mark_no
+    note: 'ось «происхождение» (ОНТОЛОГИЯ-АУДИТ часть 3): И — внутреннее · В — внешнее · П — предположение; ставит источник/документ; прежнее имя source_mark снято'
+    label: Происхождение
+  - name: mark_no
     type: str
     required: false
-    note: 'номер источника из списка документа: И1, В10'
+    note: 'номер источника из списка документа: И1, В10 (прежнее имя source_mark_no)'
     label: Номер источника
   - name: evidence
     type: enum[claimed,corroborated,measured,assumed]
     required: true
-    note: ''
+    note: 'ось «свидетельство» (ОНТОЛОГИЯ-АУДИТ часть 3): заявлено · подтверждено вторым источником · измерено · допущение; ставит система по сверке; уверенность (confidence) — ось ПРЕДЛОЖЕНИЯ, у факта её нет'
     label: Свидетельство
-  - name: confidence
-    type: num 0–1
-    required: true
-    note: ''
-    label: Уверенность
   - name: as_of
     type: date
     required: true
@@ -3457,10 +3424,10 @@ kinds:
     required: true
     note: наследуется от источника
     label: Гриф
-  - name: authority
+  - name: rank
     type: enum[mandatory,expert,reference,doubtful]
     required: true
-    note: наследуется от документа; expert — ручной ввод; ранг подсказывает в противоречии, не выбирает победителя
+    note: ось «доверие» (ОНТОЛОГИЯ-АУДИТ часть 3) — наследуется от документа; expert — ручной ввод; ранг подсказывает в противоречии, не выбирает победителя; прежнее имя authority снято
     label: Ранг доверия
   - name: parse_run
     type: ref parse_run
@@ -3930,7 +3897,7 @@ kinds:
   - name: result_material
     type: ref material
     required: false
-    note: authority=doubtful до подтверждения источников человеком
+    note: rank=doubtful до подтверждения источников человеком
     label: Материал результата
   - name: accepted_summary
     type: '{stakeholders,programs,norms,applications,analogs}'
@@ -4235,7 +4202,7 @@ knowledge_field_rules:
 - новая версия источника → superseded + пометы, диспозиции не трогаются
 - rejected не удаляется
 - к PDR принятые величины ≥ corroborated
-- authority факта наследуется от материала; ручной ввод эксперта — expert (источник — учётка, роль, дата; якоря нет)
+- rank факта наследуется от материала; ручной ввод эксперта — expert (источник — учётка, роль, дата; якоря нет)
 - противоречие показывает ОБА значения с рангами; ранг подсказывает, победителя выбирает человек
 - 'синтез не переписывает принятое: только предложение с происхождением n:m и основаниями'
 - результат исследования — материал doubtful; reference только после подтверждения источников человеком
@@ -4379,7 +4346,7 @@ enum_labels:
     context: обстановка
     supplier: поставщик
     heritage: наследие
-  material.authority:
+  material.rank:
     mandatory: обязательный
     expert: экспертный
     reference: справочный
@@ -4392,7 +4359,7 @@ enum_labels:
     rejected: отклонён
     contested: оспорен
     superseded: устарел
-  fact.source_mark:
+  fact.mark:
     И: внутреннее
     В: внешнее
     П: предположение
@@ -4492,8 +4459,9 @@ aoa_rule: 'сравнение вариантов: строки — вариан
 scene_responsible_rule: условие «ответственные сцен» читает окна плана; незаполненное — умолчание из ролей проекта; назначение — на A1/сцене 1 и в паспорте
 manual_fact_rule: 'ручной факт заводится экраном поля знаний только через сверку (СВЕРКА-РУЧНОГО-ВВОДА): предложения под строкой, решение человека; ворота «ввод не сверен» верны, обход их запрещён'
 assumption_register_rule: 'реестр допущений (MCReport §10, SEMP) — факты с disposition=assumed, независимо от метки происхождения: метка «П» говорит, что источник предположил; допущением факт становится
-  решением (владелец + точка). Факт «П» без диспозиции — кандидат в допущения, в §10 не печатается; запросы шаблонов читают disposition, не source_mark'
+  решением (владелец + точка). Факт «П» без диспозиции — кандидат в допущения, в §10 не печатается; запросы шаблонов читают disposition, не mark'
 external_system_rule: 'внешняя система — не узел и не сторона: именованный объект вне состава с владельцем-стороной; встречается как сторона стыка и участник сценария'
+interest_to_need_rule: 'интерес стороны — чего она хочет от проекта, её словами (stakeholder.interest[]): не роль и не нужда; нуждой становится только решением человека — кандидат need [П] с формулировкой интереса и стороной-носителем идёт через сверку (предложение, не автомат)'
 
 # Понятия постановки — одна истина вместо трёх (ОНТОЛОГИЯ-АУДИТ-И-МОДЕЛЬ, часть 7;
 # ЗАДАНИЕ-ШИП-4 §1, 24.09). У понятия — его вид (kinds выше) и колонки: границы
@@ -4508,7 +4476,7 @@ concepts:
     fields:
       name: subject
       role: по предикату
-      interest: из facts kind=assessment|framing о subject
+      interest: список из facts kind=assessment|framing о subject, каждый с цитатой; нужда из интереса — решением (interest_to_need_rule)
       scale: quantity о subject
     must_link: []
     conflict_on:
@@ -4539,7 +4507,8 @@ concepts:
   need:
     fields:
       statement: предикат+объект
-      stakeholder: subject (owns) — обязательно
+      stakeholder: subject (owns) — обязательно; в кандидате — имя ОДНОЙ стороны, у принятой нужды носителей много
+        (stakeholders[]) — та же формулировка у другой стороны дополняет нужду носителем, а не заводит копию
       qos_class: множество классов из текста (A′ · B′ · C′, один или несколько); сервис уточняет один
       coverage_expected: 'чем нужда закрывается: service · constraint · programme · requirement; по умолчанию из
         роли носителя (заказчик/оператор/потребитель/учреждаемый → service; регулятор → constraint; поставщик/партнёр
@@ -4548,12 +4517,11 @@ concepts:
     - owns→stakeholder
     conflict_on:
     - statement contradicts
-    note: интерес стороны → нужда [П], не [И]
+    note: интерес стороны → нужда [П], не [И]; одна нужда — много носителей (n:m), копий по сторонам нет
     identity:
       key:
-      - stakeholder
       - statement_core
-      semantic: та же потребность иными словами; отличие — охват, число, класс
+      semantic: та же потребность иными словами; отличие — охват, число, класс; носитель ключом не является
       threshold: 0.8
     not_is:
     - роль стороны (поле role)
@@ -4567,7 +4535,7 @@ concepts:
       интерес это нужда-кандидат [П], (в) прямых формулировок нехватки в прозе'
     distribution_rule: общая нужда (перечень §2.1) раздаётся сторонам с ролями заказчик · оператор · потребитель
       · учреждаемый, чей интерес или сфера о том же предмете; поставщикам и регуляторам общие нужды не достаются
-      — их нужды только из собственного интереса
+      — их нужды только из собственного интереса; раздача — носителями одной нужды, не копиями
     answer_shape: нужды живут внутри стороны (stakeholders[].needs[]), у каждой ОБЯЗАТЕЛЬНЫЙ свой id вида p1.n2
       — на него ссылаются цели и сервисы; ответ без id у нужды — недействителен
     coverage_rule: выход сцены 6 «каждая нужда покрыта сервисом» считает только нужды с coverage_expected=service;
@@ -4840,7 +4808,7 @@ formation:
     contradict: совпадение по ключу; различие в conflict_on → contested с рангами обоих
     confirm: совпадение полностью из независимого документа → evidence=corroborated
     manual_edit: правка принятого без факта → предложить ручной факт [И] или помету «без основания»
-    manual_input: 'ручной ввод → кандидат-факт authority=expert → та же сверка: дубль (ключ, затем семантика по
+    manual_input: 'ручной ввод → кандидат-факт rank=expert → та же сверка: дубль (ключ, затем семантика по
       срезу понятия) · противоречие (conflict_on после нормализации) · соединение (supports/refines, must_link)
       · нехватка (must_link) — предложения с уверенностью и полем отличия; выполняет только человек'
     batching: несколько ручных вводов — один вызов сверки с задержкой; срез ≤ 50 фактов и сущностей
