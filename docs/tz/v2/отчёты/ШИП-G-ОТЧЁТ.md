# Шип G — Phase A: отчёт (2026-09-10)

Задание: `ЗАДАНИЕ-CODE-БОЛЬШОЕ` §Шип G. Решения — ADR-067 (Phase A), ADR-066
(владелец выступает от имени роли — по `ШИП-F-ЗАКРЫТ`). Формат — пункт ·
сделано · где проверено.

## Пункты шипа

| № | Пункт задания | Сделано | Мера |
|---|---|---|---|
| 1 | Шаблон Phase A: 12 задач → сцены с мероприятиями стадии 2 Романова, дорожка моделирования, `instantiate_per`, связи FS/SS/FF/INPUT с обоснованиями | `tools/v2/gen_phase_a_template.py` → `ШАБЛОН-ФАЗЫ-PHASE-A-NASA.json` (`PHT-9002`): сцены A1–A10 = задачи 1–10 БП-PA (шаги, входы/выходы словарём готовности), мероприятия — блоки 2.x · 2.E* · 2.M* · 2.ME* · 2.P · 2.PE Романова, дорожки `modeling` и `management`; **A4 «Аванпроект элемента {node}» — `instantiate_per: element`** (движок разворачивает сцену в экземпляр `A4:<код>` на каждый узел вида `element`; без узлов сцена одна и говорит, где их завести; сцена шаблона прожита, когда прожиты все экземпляры); связи `depends` FS · SS · FF · INPUT с `why`, SS обслуживает новое условие `scene_started`; точки — задача 11 строгой цепочкой: внутренний обзор → SRR (EXP-2A) → SDR/MDR (EXP-2B, 44 позиции, 7 сопоставлены нашим видам) → KDP-B (матрица Д1–Д10). Переход — данными: `KDP-A` Pre-A несёт `opens_template: PHT-9002`, решение записывает проекту `phase_template` и заводит записи точек новой фазы. Сторож в CI: шаблон совпадает с генератором | `PhaseATest` (3): после KDP-A лента A1–A10 и точки Phase A, A4 без элементов LOCKED словами, A2 открыта по SS от начала A1; экземпляры `A4:GS`, `A4:SC`, выходы `node_requirements_min:{node}` / `node_interfaces_min:{node}`, SDR ждёт `scene_done:A4` до последнего экземпляра |
| 2 | SEMP 11 разделов (§2 из полки нормативов, §6 — 17 процессов из `process_catalog`, §8 TPM из свёрток); OpsCon; ICD стыка из параметров и требований | `tools/v2/gen_process_catalog.py` → **полка `PRC-9001`** (17 процессов NPR 7123.1 из конфигурации SEMP первой версии: группа, ISO 15288, мероприятия Романова, механизм «Орбиты», место, tailoring названо). `tools/v2/gen_phase_a_documents.py` → `ШАБЛОН-SEMP.json` (11 разделов; §2 и §6 — запросы **в область полки** `area: library`, §6 развёрткой `processes`, `min_rows 17`; §8 свёртки бюджетов; §9 отклонения; §10 инструменты; §11 согласование), `ШАБЛОН-OPSCON.json` (режимы · сценарии · окна сеансов · нештатные регламенты из рисков · переходы), `ШАБЛОН-ICD.json` (реестр стыков · параметры с `target_kind: interface` · требования с `carrier_kind: interface` · элементы обмена · ответственность). Запросы документов умеют область полки и природу носителя/цели; сторож полей `where` пополнен | `PhaseADocumentsTest` (2): **SEMP §6 — 17 строк** с полки, отклонение названо, §2 видит норматив полки, 11 разделов; **ICD: реестр 1 стык, параметры только стыка (масса КА не идёт), требования только с носителем-стыком** |
| 3 | Системные требования из проектных деривацией; распределение по природе носителя; условное базирование по TRL; suspect после правки | `Requirements.derive(parent, statement, rationale, carrier, subtype)`: уровень `system`, источник — родитель, связь `derives_from` с видом decomposition · derivation · refinement и обоснованием — без «почему» отказ; `POST /v2/requirements/derive`; условия `system_requirements_min`, `each_system_requirement_derived`, `node_requirements_min:<узел>:<n>`, `node_interfaces_min:<узел>:<n>` в контурах требований и архитектуры. Природа носителя, базирование по TRL и подозрительные связи — существующие механизмы шипов C–D (`carrier_nature_ok`, `EntityBaselines` держит снимок технологией с `trl_current < trl_required`, `no_suspect_links`), в Phase A они входят условиями точек SRR/SDR | `PhaseATest`: отказ деривации без основания, `RQ-S-0001` с `derives_from{decomposition}`, маршрут 201. Существующее — `RequirementsTest`: «требование без носителя и носитель чужой природы — отказ», «правка после снимка делает связь подозрительной, подтверждение снимает», «подозрительная связь после базирования держит точку» |
| 4 | Capella-структуры fixture с баннером; экран «Внешняя модель» | `ArchitectureFactory.externalModel`: адаптер Capella по `ORBITA_CAPELLA_URL` (`GET /elements`), иначе учебная fixture из ресурса модуля (28 элементов OA/SA/LA/PA в форме адаптера) с баннером «учебная выборка, не доказательство интеграции»; соответствие узлам состава по `external_identity`; `GET /v2/external-model?project=`; экран «Внешняя модель» в эксперт-режиме v2 (слои, соответствия, несопоставленные) | Живьём на стенде (экран) — см. «Стенд» |
| — | Клиент | Лента Phase A: экземпляр «аванпроект элемента» с узлом, строка связей «после A1 (SS) — …»; `POST /v2/interfaces` (стык кодами узлов), `GET /v2/interfaces`; реестр стыков в прогоне | `walk_scenes.py --phase-a` |

## Владелец выступает от имени роли (ADR-066, `ШИП-F-ЗАКРЫТ`)

`POST /auth/act-as {role}` — РП · ведущий СИ · инженер · DA, пустая роль —
своя; только владельцу системы (учётка Telegram из `ORBITA_ADMIN_TIDS` либо
`chernov` в режиме stand), остальным 403. Выбор живёт в сессии (`V046`),
роль точная (без добавки «руководитель несёт DA»), автор каждого действия —
«Чернов Д. как инженер» в провенансе v1 и v2, `whoami` отдаёт `author` ·
`acting_role` · `can_act_as`; селектор в шапке — только владельцу. Тест
`ActingRoleTest`. Инструменты стенда ходят той же дорогой: на стенде за
входом через Telegram `walk_scenes` · `load_shelves` · `check_tor_mode` ·
`check_exchange` входят через бота одной учёткой владельца (ссылка в консоль,
подтверждение в Telegram, сессия сохраняется в `~/.config/orbita`), а
«учётки» chernov · ivanov · petrova становятся ролью «от имени»
(`tools/v2/stand_session.py`). **Находка `ci/checks.sh`:** логин `tg:<id>`
не проходил проверку логина В3 — первый вход через Telegram на 216 упал бы
на INSERT; миграция `V047` допускает второй вид логина явно.

## Мера задания

| Мера | Как проверена | Итог |
|---|---|---|
| переход KDP-A → Phase A на проекте прогона | `PhaseATest` (решение KDP-A → шаблон PHT-9002, точки заведены); живьём — `walk_scenes.py --points --phase-a` | тест зелёный; живьём — «Стенд» |
| лента Phase A с экземплярами «аванпроект элемента A/B/C» | `PhaseATest` (A4:GS, A4:SC); прогон заводит EL-SC · EL-GS · EL-UT | тест зелёный; живьём — «Стенд» |
| SEMP §6 — 17 строк | `PhaseADocumentsTest`; прогон печатает число строк §6 | тест зелёный; живьём — «Стенд» |
| ICD `IF-S-USER` собран из стыка | `PhaseADocumentsTest`; прогон заводит стык IF-S-USER, параметр и RQ-S-0004 на нём | тест зелёный; живьём — «Стенд» |

## Что осталось и что названо

- `instantiate_per: segment` шаблоном не используется: аванпроект сегмента
  (2.M*) идёт мероприятиями сцены A4 элементов; механизм общий — сцена на
  сегменты заводится одной строкой шаблона, когда владелец скажет, нужна ли
  отдельная лента по сегментам.
- OpsCon живьём не измерялся (меры задания его не называют); разделы
  собираются из режимов, сценариев и рисков прогона.
- Коды зрелости EXP-2B (С · Р · F · U · Σ · Δ · x · °) — до легенды владельца
  (помета `maturity_note` в шаблоне).
- Ответ на вопрос `ШИП-F-ЗАКРЫТ` (четыре дыры `check_tor_mode` на настоящем
  ТЗ) — одной строкой после прогона на стенде с ключом модели: локальный
  стенд ждёт диска, 216 — ключа в `.env`.

## Стенд

Заполняется после выката (`deploy_orbita_server`) и прогона
`tools/v2/walk_scenes.py --base <216> --project <новый> --points --phase-a`.
