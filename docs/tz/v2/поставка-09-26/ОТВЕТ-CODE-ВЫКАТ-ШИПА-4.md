# Ответ Code по выкату шипа 4 (25.09, HEAD `26bc51b`)

## 1. Правка истины `synthesis_run.trigger += reading` — подтверждена

Значение верно по смыслу: чтение документа в постановку одним вызовом (РЕШЕНИЕ-ЧИТАТЬ-СМЫСЛ) — законный триггер синтеза, отличный от `manual` и `new_facts`. Помета в `note` с датами и причиной («сторож записи 22.09 его отбивал») — правильная форма для правки Code в истине: так и впредь. Отдельного диффа владельца на это не будет — HEAD и есть истина.

Истина с `26bc51b` синхронизирована во внешний контур: 95 видов, 639 полей, 114 перечислений. Всё остальное, что изменилось против копии до шипа 4, — по заданию §1 (вид `step` снят по §1.8, `confidence` только у предложения по §1.6, `authority → rank`, `source_mark → mark`, `stakeholders[]` у нужды, понятия и правила формирования внутри истины). Незапрошенных правок, кроме `reading`, не найдено.

## 2. Выкат принят. Из трёх условий выката закрыто одно с половиной

| Условие | Состояние | Что дальше |
|---|---|---|
| Три регрессии КТ2 | словарь — закрыт («Кандидаты» вкладкой); **верхнее меню Постановки** и **группировка фактов** — отложены «на дизайн» | не закрывать этим шипом — идут в КТ2 дизайн-обход, но в NEXT.md держать как долг, не как замечание |
| Ворота «устав один» на `PATCH /v2/entities/{код}` (поле `role` у материала) | **открыто** — «клиент ею не ходит» | закрыть следующей поставкой: тот же сторож, что на загрузке; тест на отказ — второй `charter` в проекте → 409 с именем первого |
| Живой прогон экранов §3 | на стенде живых вызовов не было (по слову владельца); проверено на копии | остаётся за владельцем — маршрут ниже |

## 3. Два дополнения, о которых напоминал

* StrictDoc на 216 — в пакете точки, не пометой в манифесте. Сейчас манифест честно говорит «StrictDoc не поднят»; это допустимо до КТ1, но не после.
* Журнал ИИ: у каждого вызова — токены (вход/выход) и секунды, включая рендер связного текста и печать. Клиент секунды уже считает — в журнал они должны ложиться сервером.

## 4. Находка из NEXT.md §0, о которой владелец должен знать

Стенд `PJ-ПМИ7` по записям **всё ещё Pre-Phase A**: у точек `internal_review`, `MCR`, `KDP-A` нет ни одного решения (все версии `draft`); единственное решение `DEC-MCR-1` было пробой Code и снято скриптом 22.09 по З-27. Линия «KDP-A — решение о переходе к Phase A · 21.09» у ConOps — имя линии, не решение точки. Чтобы стенд показал фазу A, три точки должен решить владелец от имени DA. Code правильно не стал фиксировать это сам.

## 5. Открытые решения владельца (не Code)

1. Эмбеддинги — локально на 216 (Ollama `bge-m3` или TEI, тот же контракт); внешний ключ не подходит из-за ДСП. До решения поиск лексический.
2. Хозяин словаря — кто принимает кандидатов: предложение — ведущий СИ; маршрут пока роль не проверяет.
3. Легенда кодов зрелости Романова; фрагмент EXP-2A; `node@22` на Mac-воркере.
