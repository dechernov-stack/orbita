--- repo
+++ new
@@ -709,0 +710 @@
+  note: критерий — проверка ворот точки (читает данные); к порогам показателей отношения не имеет
@@ -1756,0 +1758 @@
+  note: 'вариант = строение (подгруппы: орбиты, плоскости, число КА) + рабочий флаг; его показатели — метрики (вид metric), значения — прогонами моделей'
@@ -2041 +2043 @@
-    note: ''
+    note: 'порог отсева: {op,value,unit}; отсеянный вариант называет порог по имени'
@@ -2045 +2047 @@
-    note: ''
+    note: 'группа сравнения: покрытие · задержка · стоимость · риск (четыре группы AoA)'
@@ -2049,0 +2052,10 @@
+  - name: direction
+    type: enum[max,min]
+    required: true
+    note: 'куда лучше: больше или меньше'
+    label: Направление
+  - name: model_run
+    type: ref model_run
+    required: false
+    note: откуда значение; пусто — ручное/прокси с пометой
+    label: Прогон модели
@@ -2051,0 +2064 @@
+  note: метрика — сущность «вариант × показатель»; сравнение и Парето читают метрики варианта, у варианта поля metrics нет; пороги — у метрики, не у критерия ворот
@@ -3805,0 +3819,2 @@
+aoa_rule: 'сравнение вариантов: строки — варианты, столбцы — метрики по группам, значение — из прогона, порог — у метрики; отсев по порогу с именем порога; Парето по направлениям метрик; итогового балла
+  нет; базовая концепция ссылается на вариант с обоснованием'