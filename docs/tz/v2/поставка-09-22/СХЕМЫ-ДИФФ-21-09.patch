--- repo
+++ new
@@ -526 +526 @@
-    note: ''
+    note: правится в паспорте проекта (карточка), не только при создании
@@ -530 +530 @@
-    note: ''
+    note: правится в паспорте проекта (карточка), не только при создании
@@ -534 +534 @@
-    note: ''
+    note: правится в паспорте проекта (карточка), не только при создании
@@ -550,0 +551,10 @@
+  - name: standard
+    type: enum[NASA-7120]
+    required: true
+    note: имена в интерфейсе; кросс-термины — словарь
+    label: Стандарт
+  - name: phase_template
+    type: ref phase_template
+    required: true
+    note: шаблон текущей фазы; меняется решением точки вместе с phase_current
+    label: Шаблон фазы
@@ -3099,0 +3110 @@
+  note: одна запись = один процесс (17 записей NPR ↔ ISO); полка хранит каталог записями, не одной записью со списком — привести полку к истине
@@ -3805,0 +3817,5 @@
+  project.standard:
+    NASA-7120: NASA (NPR 7120.5)
+write_rules:
+  schema_on_write: 'любая запись сущности валидируется истиной: поле вне схемы вида — отказ, а не молчаливое сохранение'
+  print_queries: запрос печати ссылается только на поля истины; неизвестное поле — отказ сборки (validate_document_queries)