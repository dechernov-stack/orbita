# 109 предложений — принято; ответ по требованиям; старт ПМИ-7

Риски 12 из 12 и замысел — на мере; стороны и сервисы — на мере;
нужды 30 из 43; вехи 12 → 7 по помете «только этап проекта».
Исполнение верное: понятия без правил по видам фактов читаются
разделом по `from`, промпт описывает их самой истиной.

## Требования 2 из 12 — Code прав, правило моё было неточным

В уставе нет раздела требований, и по нашей же модели требования
уровня проекта **не читаются, а образуются**: на сцене 8, из принятых
целей, нужд и ограничений, решением инженера. Двенадцать RQ-P — это
ожидание для сцены 8, не для чтения документа. Онтология поправлена:
из устава требования читаются только при явном разделе; из ТЗ —
читаются. Мера чтения записки — 0 требований; мера сцены 8 — 12.

## Три хвоста — закрыть, все три из истины

1. `id` у нужды внутри стороны — **обязателен** (`p1.n2`); ответ без
   него недействителен. Сервис и цель ссылаются на него.
2. Любая величина — объектом `{value|min,max,unit}`: показатель,
   `target_measure`, масштаб, диапазон этапа, граница. Строка с
   числом — брак ответа; это одно правило нормализации, не три.
3. Нормативы — перечень §2.2 читается построчно: строка → норматив.

Нужды 30 из 43 — для старта достаточно; недобор закрывается добором
из эталона предложениями.

## Старт ПМИ-7 — с одной поправкой в сценарии

Постановка — **чтением записки контуром**, массовый приём; чего контур
не добрал против эталона — **добор из `ПОСТАНОВКА-ИЗ-ЗАПИСКИ.json`**
предложениями; сам эталон — страховка на случай срыва чтения.
Остальное по сценарию: только блокеры, минуты руками/с ИИ по сценам,
восемь выходов. Предусловия Code — как в сценарии, плюс три хвоста.
