# Подготовка к ручному прогону (§2 ПМИ) — отчёт

Заполняется до сессии. Инженеру этот файл читать не обязательно: он нужен
ведущему испытаний, чтобы знать, на чём и чем проводится прогон.

## Версия стенда

| Что | Значение |
| --- | --- |
| Ветка / коммит | `main` @ `5de4cbb` (развёрнуто 2026-08-23) |
| Как собран | штатный workflow оркестратора `deploy_orbita_local` (сборка из `origin/main`, health-проверка) |
| Состояние данных | **пусто**: без демо-профиля (`--profile demo` не поднимался) и без затравки `data/pilot` |
| Адрес | http://localhost:8080 |
| Служба ИИ | прямой канал настроен (`ORBITA_AI_KEY` в `.env`), модель `claude-sonnet-5`, потолок ответа 32000 токенов |

Проверка «пусто» перед сессией:

```bash
curl -s "http://127.0.0.1:8080/api/objects?type=project"   # ожидается []
```

## Пакеты материала

Лежат в `packets/`. Пакеты 01–04 — **формат канала ИИ** (массив предложений:
именно так их вернула бы модель, и именно так инженер вставляет их в экран
«Служба ИИ» → «Ответ закрытого контура»). Пакеты 00 и 05–07 — **формат
загрузки пачкой** (`{"objects": [...]}`, экран «Загрузка пачкой»).

| Файл | Что внутри | Формат | Шаг ПМИ |
| --- | --- | --- | --- |
| `00-профили-службы.json` | профили службы ИИ: порождающий и рецензионный | импорт | 2 |
| `01-цели.json` | цели и задачи миссии с MOE | канал ИИ | 3 |
| `02-нужды.json` | нужды стейкхолдеров | канал ИИ | 4 |
| `03-сервисы.json` | сервисы с QoS-профилями по классам | канал ИИ | 5 |
| `04-требования.json` | системные требования с MOP и планами верификации | канал ИИ | 6 |
| `05-входы-моделирования.json` | группировка, аппарат, карта спроса, станции, адаптер, профиль терминала, сценарий | импорт | 13 |
| `06-материал-pre-phase-a.json` | состав системы и интерфейсы, WBS, альтернативы и descope, оценки стоимости, ODA, риски, технологии, ConOps | импорт | 7 |
| `07-замечания-обзора.json` | критическое замечание обзора MCR | импорт | 10 |

### Чем собраны

* **01–04** — **живой моделью** через службу: `POST /api/ai/ask`, профиль
  `AP-0201` (запреты Р1–Р9, глоссарий, правило основания), промпт собирает
  служба. Сохранено то, что **прошло фильтр** (`shown`): предложения с
  замечаниями в пакеты не попали. Счёт по каждому вызову — в журнале службы
  проекта подготовки и в `packets/сводка.md`.
* **05** — выгрузка эталонного демо-проекта (`GET /api/export/objects`):
  инструмент моделирования отдаёт входы тем же форматом, второй копии чисел
  нет.
* **06, 07** — материал репетиции (`web/mission-material.json`), проверенный
  прогоном `mission-run`.

### Счёт объектов (проверено `packets/проверить.py`, замечаний 0)

* `00-профили-службы.json` — 2
* `01-цели.json` — 8
* `02-нужды.json` — 24
* `03-сервисы.json` — 12
* `04-требования.json` — 212
* `05-входы-моделирования.json` — 7
* `06-материал-pre-phase-a.json` — 40
* `07-замечания-обзора.json` — 1

Всего 306 объектов. Требований — 212
(7 уровня проекта, 205 системных) при пороге ПМИ «не менее 200».

### Журнал подготовки

Собрано прямым каналом, модель `claude-sonnet-5`, профиль `AP-0201`:
вызовов службы 65, предложено 400+, снято фильтром 24 — из них **14 правилом
основания** (значение без ссылки на источник возвращается инженеру). Генерация
один раз прервалась исчерпанием баланса провайдера: отказ записан в журнал
с причиной, собранное сохранено, добор довёл пакет до порога.

## Что ведущий испытаний делает перед сессией

1. Поднять стенд штатным workflow и убедиться, что портфель пуст.
2. Проверить прямой канал службы: `POST /api/ai/ask` отвечает без отказа
   (при незаполненном ключе — 503 с причиной, это тоже допустимое условие
   сессии: тогда инженер работает режимом закрытого контура и вносит
   пакеты 01–04 вручную — путь ПМИ от этого не меняется).
3. Открыть `ПМИ.md` и приготовить `shots/` для снимков.
