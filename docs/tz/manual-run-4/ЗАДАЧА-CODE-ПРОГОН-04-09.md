# Задача Code: находки прогона ПМИ-4 (04.09) — до возобновления с В1

Прогон остановлен: дальше идти некуда. Источники: `ЗАМЕЧАНИЯ-Б5.md`,
`ЗАМЕЧАНИЯ-В-Г.md`, снимки 13:35 · 14:18 · 14:25. Порядок — по
блокирующей силе; каждый шип — тег, прогон, снимок под нужной учёткой.

## Шип 0 · Блокеры прогона

**0.1 Задания — экран, маршрут, отклик.** Счётчик «мои 7» в шапке
показывает, что задания создались; но экрана «Мои задания» нет, ссылка
«Чернов увидит в „Мои задания"» — в никуда, назначение группы не даёт
отклика. Сделать: раздел «Работа» получает разрез **«Мои задания»**
(вид `/views/my-tasks`: цель-разрыв · срок · откуда назначено · «к
месту»); счётчик в шапке — кликабелен и ведёт туда же; после назначения
— тост «назначено N → ‹кто›» с ссылкой; ошибка — с причиной. Выяснить,
почему сторож мёртвых ссылок пропустил эту (ссылка собрана строкой?)
и закрыть дыру сторожа.

**0.2 Гант — картинка.** Клик, hover, drag, строки таблицы — ничего.
Проверить по порядку: `readonly` из-за роли под `auth_mode=stand`;
обработчики не привязаны после таблицы слева; строки не цели клика.
Мера под каждой учёткой: клик → рамка задачи; hover — попап с
действиями; drag у РП пишет план, у инженера — отказ по праву текстом.

**0.3 Фаза чипа против фазы данных.** Проект создан на А2 как Pre-A;
шапка и лента показывают Phase A (SRR · SDR · KDP-B). Найти, кто сдвинул
фазу (мастер? профиль? сид?), и запретить смену фазы любым путём, кроме
фиксации KDP уполномоченным. Если это проект-пример PJ-0002 — сказать,
и тогда дефект другой: у примера должна быть своя честная фаза.

**0.4 Файл под чужой карточкой** (Б5-01): `file_ref` против хеша;
тест «три документа — три ссылки без пересечений»; если запись неверна
— канон Д1 пересобрать.

## Шип 1 · Рамка ведения: шаг должен говорить, что делать

Снимок 14:25 — шаг «План работ фазы»: подсказка есть, встроен весь
экран «Жизненный цикл» с датами и кнопкой DA «Зафиксировать
прохождение», а что должен сделать пользователь — неясно, «Дальше»
серая без причины.

1. **Панель условий шага** над встроенным экраном: «✓ даты вех заданы
   3/3 · ☐ плановые окна задач заданы 0/12 → к ленте · ☐ ответственные
   задач 0/12 → назначить» — вычислено, каждая строка ведёт к месту;
   шаг закрывается сам, когда всё ✓.
2. **Встроенный экран — фрагментом под шаг**, не целиком: для «Плана
   работ» — даты вех и лента, без кнопки фиксации DA (она — в шаге
   «Точки фазы», и только у DA); чужие первичные действия внутри шага
   не показываются.
3. **«Дальше →» серая — с причиной и переходом** (правило disabled):
   «шаг не закрыт: окна задач не заданы → к ленте».
4. Нить потока и степпер — оставить, они работают.

## Шип 2 · Действия с пакетами и назначения

- **П-01** предпросмотр промпта: «Скопировать» и «Скачать .md» (шапка
  md: вид · отпечаток · схема ответа).
- **П-02** после вставки пакета: поля в правке с акцентом изменённого,
  одна первичная кнопка «Принять ‹замысел/пакет›», вторичная «Отменить
  вставку» — правило для всех вставок.
- **П-03** назначение исполнителя/владельца — `RefPicker` по
  справочнику учёток везде (разрывы, задачи, шаги, риски); строкой
  ввести нельзя.
- **Б5-03** старые кнопки «Разобрать: профили/риски» на карточке
  документа — убрать; одно действие «Разобрать документ»; для типа
  «справка» не предлагается.

## Шип 3 · Сторожа классов (каждый — тест в CI)

1. `undefined` / `null` / `NaN` в DOM — 0 (Б5-02).
2. Латинские служебные имена в тексте UI — 0: «(conops)»,
  «(requirement)», «3 AP · 5 FC · 23 FN · 8 LC · 6 OC · 9 SK · 14 SM» в
  паспорте — по-русски из глоссария видов («3 профиля · 5 цепочек ·
  23 функции · 8 логических компонентов · 6 способностей …»).
3. Обрезки и переносы статусов — 0: «назначить группу · 8…»,
  «карточка заполнена ✓» в три строки (Б5-04).
4. Disabled без причины — 0 (линт подсказок).
5. Мёртвая ссылка — 0, включая ссылки, собранные строкой (дыра из 0.1).

## Шип 4 · Живой контур

**Ж-01** «Overloaded»/обрыв потока: автоповтор 3× (2·4·8 с),
идемпотентно; одно сообщение с кнопками «Повторить · Вставить пакет»;
запись в журнал службы с причиной и токенами.

## Приёмка — Code проходит В1–В8 и Г1–Г8 сам, под тремя учётками

- [ ] В1–В8: лента кликабельна, план тянется у РП, отказ у инженера,
      назначение → тост → «Мои задания» у инженера → закрытие разрыва
      гасит задание (Г8);
- [ ] рамка шага «План работ»: панель условий, фрагмент без DA-кнопки,
      «Дальше» с причиной, шаг закрылся сам после окон;
- [ ] фаза проекта прогона — Pre-A везде (чип, лента, задачи);
- [ ] три документа — три ссылки; `undefined` — 0; служебных имён — 0;
      обрезков — 0;
- [ ] промпт копируется и скачивается; после вставки Р01 — одна
      акцентная кнопка «Принять замысел»;
- [ ] подложенный 529 — три попытки, сообщение с двумя кнопками.
Снимки под именами шагов — в `артефакты/`; отчёт таблицей «пункт ·
сделано · тег · вопросы».

## Ловушки

1. Не чинить симптом без класса: каждый пункт шипа 3 — сторож, не
   правка. 2. Задания — не новый вид; вывести существующий вид на
   сцену. 3. Фазу не «поправить руками» на стенде — найти причину.
4. Внутри рамки шага — никаких чужих первичных действий.
