# ПМИ-4 — журнал прохода (действие → результат → артефакт)

Задание: `ПМИ-4-ПРИЁМКА.md`. Токены: живых вызовов службы — 0; все ИИ-шаги —
вставкой пакетов из `пакеты/` (реестр — `пакеты/реестр.md`). Артефакты
шагов — в `артефакты/` под именем шага.

## Предусловия (Code)

| Пункт | Состояние |
|---|---|
| Шипы 0–3 «трёх пакетов» выкачены | да: `mvp-p5-27`, `mvp-p5-28` + догоняющие коммиты |
| Резервная копия стенда | `~/Projects/orbita-backups/pmi4-start-*.dump` (pg_dump -Fc) + `files-*.tgz` — вне репозитория |
| Стенд | проекты, кроме PJ-0001, удалены 2026-09-02; полки LIB целы |
| Режим стенда `auth_mode=stand` | выкачен и проверен: whoami отдаёт учётки с ролями, вход без пароля, селектор в шапке переключает учётку; Петрова на плане Ганта получает отказ «план работ фазы ведёт руководитель проекта (ваша роль — specialist)» |
| Пакеты Р01–Р05 | `пакеты/`, проверены на тестовой базе (см. реестр) |
| Тег `пми4-старт` | ставится после выката режима стенда |

## Учётки стенда (auth_mode=stand)

| Учётка | Логин | Роль в новом проекте |
|---|---|---|
| Чернов Д. | chernov | руководитель проекта (lead) — планы, назначения, фиксация точек |
| Иванов И. | ivanov | ведущий СИ (lead_se) |
| Петрова М. | petrova | инженер (specialist) |

Переключение — селектором в шапке; пароль не спрашивается. Режим включается
переменной окружения `ORBITA_AUTH_MODE=stand` только на приёмочном стенде.

## Ход прохода

Строка на шаг: `№ · учётка · результат · артефакт · находки`.

### А. Портфель и создание

(заполняется владельцем по ходу)

### Б. Мастер-путь

### В. Работа фазы (Pre-A) — Гант

### Г. Постановка

### Д. Требования

### Е. Состав и модели

### Ж. Библиотека

### И. Документы (Pre-A)

### К. Точка MCR и переход

### Л. Phase A — Работа

### М. SEMP

### Н. Phase A — задачи 3–11

### О. Точки фазы и итог

## Находки

(дефекты по ходу — номером шага, чинятся отдельными коммитами)
