# Реестр маршрутов клиента

Собран из кода (`tools/routes_registry.py`): рейка — `web/src/nav.ts`, экраны —
`web/src/App.tsx`. Адреса контент-поставок (поле `screen` задач фазы, чек-листов,
операций) берутся отсюда, не на слух; сторож сида сверяет их с этим же кодом.

| Экран (`screen`) | Заголовок | Как открывается |
|---|---|---|
| `phasework` | Работа фазы | рейка «Работа» → «Работа фазы» |
| `portfolio` | Все проекты | рейка «Портфель» → «Все проекты» |
| `sourcedocs` | Исходные документы (библиотека) | рейка «Библиотека» → «Материалы проекта» |
| `docparse` | Разбор документов | рейка «Библиотека» → «Разбор документов» |
| `shelves` | Полки | рейка «Библиотека» → «Полки» |
| `libknowledge` | Знание полки | рейка «Библиотека» → «Знание полки» |
| `references` | Справочники | рейка «Библиотека» → «Справочники» |
| `results` | Результаты | рейка «Библиотека» → «Результаты» |
| `goals` | Цели и нужды | рейка «Постановка» → «Цели и нужды» |
| `stakeholders` | Стейкхолдеры проекта | рейка «Постановка» → «Стейкхолдеры» |
| `needs` | Нужды и их сервисы | рейка «Постановка» → «Нужды и их сервисы» |
| `services` | Сервисы и QoS | рейка «Постановка» → «Сервисы и QoS» |
| `conops` | Сценарии ConOps | рейка «Постановка» → «Сценарии ConOps» |
| `req` | Реестр требований | рейка «Требования» → «Реестр требований» |
| `matrix` | Матрицы | рейка «Требования» → «Матрицы» |
| `trace` | Трассировка и impact | рейка «Требования» → «Трассировка и impact» |
| `aoa` | Альтернативы и решения | рейка «Концепция» → «Альтернативы и решения» |
| `functions` | Функции и цепочки | рейка «Концепция» → «Функции и цепочки» |
| `wbs` | Элементы и интерфейсы | рейка «Концепция» → «Элементы и интерфейсы» |
| `external` | Внешняя модель | рейка «Концепция» → «Внешняя модель» |
| `composition` | Дерево состава | рейка «Концепция» → «Дерево состава» |
| `wbstree` | Структура работ (WBS) | рейка «Концепция» → «Структура работ (WBS)» |
| `cost` | Оценки стоимости и сроков | рейка «Концепция» → «Оценки стоимости» |
| `sysmodels` | Модели системы | рейка «Модели» → «Модели системы» |
| `siminputs` | Входы моделирования | рейка «Модели» → «Входы моделирования» |
| `spacecraft` | Модель аппарата | рейка «Модели» → «Модель аппарата» |
| `demand` | Карта спроса | рейка «Модели» → «Карта спроса» |
| `ground` | Наземный сегмент | рейка «Модели» → «Наземный сегмент» |
| `ballistics` | Глобус | рейка «Модели» → «Глобус» |
| `coverage` | Карта покрытия | рейка «Модели» → «Карта покрытия» |
| `constcompare` | Сравнение построений | рейка «Модели» → «Сравнение построений» |
| `compare` | Сравнение вариантов | рейка «Модели» → «Сравнение вариантов» |
| `docs` | Документы | рейка «Документы» → «Комплект фазы и выпуски» |
| `lifecycle` | Жизненный цикл | рейка «Контроль» → «Жизненный цикл» |
| `readiness` | Готовность к точке | рейка «Контроль» → «Готовность к точке» |
| `mytasks` | Мои задания | рейка «Контроль» → «Мои задания» |
| `operations` | Порядок работы | рейка «Контроль» → «Порядок работы» |
| `projreg` | Паспорт проекта | рейка «Контроль» → «Паспорт проекта» |
| `rfa` | Замечания обзора (RFA/RID) | рейка «Контроль» → «Замечания обзора» |
| `inspection` | Инспекция обзора | рейка «Контроль» → «Инспекция обзора» |
| `risks` | Риски | рейка «Контроль» → «Риски» |
| `riskmatrix` | Матрица рисков | рейка «Контроль» → «Матрица рисков» |
| `trl` | Технологии и TRL | рейка «Контроль» → «Технологии» |
| `oda` | Оценка орбитального засорения | рейка «Контроль» → «Орбитальное засорение» |
| `vv` | Верификация и валидация | рейка «Контроль» → «Верификация и валидация» |
| `system` | Система в целом | рейка «Контроль» → «Система в целом» |
| `seeddemand` | Затравка спроса | рейка «Инструменты» → «Затравка спроса» |
| `aiservice` | Служба ИИ | рейка «Инструменты» → «Служба ИИ» |
| `aiprofiles` | Профили службы | рейка «Инструменты» → «Профили службы» |
| `ai` | Предложения ИИ | рейка «Инструменты» → «Предложения ИИ (контур пакетов)» |
| `importb` | Загрузка пачкой | рейка «Инструменты» → «Загрузка пачкой» |
| `terminals` | Импорт терминалов | рейка «Инструменты» → «Импорт терминалов» |
| `newproject` | — | только переходом (кнопка, ссылка, шаг задачи) |
| `startpath` | — | только переходом (кнопка, ссылка, шаг задачи) |

Экранов в клиенте: 54; в рейке: 52; только переходом: 2.
